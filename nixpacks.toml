# Nixpacks configuration for MCP server deployment
# Start command is explicit because root package.json has no "start" script

[variables]
NODE_ENV = "production"

[start]
cmd = "node apps/mcp-server/dist/index.js"
