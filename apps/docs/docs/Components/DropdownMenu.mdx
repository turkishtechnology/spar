---
title: DropdownMenu
description: Accessible dropdown menu component with keyboard navigation, typeahead search, nested submenus, and complete ARIA support.
---

import React from 'react';
import LiveCode from '../../src/components/LiveCode';
import AnatomyViewer from '../../src/components/AnatomyViewer/AnatomyViewer';
import {
  DropdownMenuRoot,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuCheckboxItem,
  DropdownMenuRadioGroup,
  DropdownMenuRadioItem,
  DropdownMenuSeparator,
  DropdownMenuLabel,
  DropdownMenuGroup,
  DropdownMenuSub,
  DropdownMenuSubTrigger,
  DropdownMenuSubContent,
} from '@turkish-technology/spar';

# DropdownMenu

Accessible dropdown menu component for creating interactive command menus with keyboard navigation, typeahead search, and full ARIA support.

![Accessibility](https://img.shields.io/badge/Accessibility-WCAG%202.2%20AA-green)
![Tree-shakeable](https://img.shields.io/badge/Tree--shakeable-Yes-blue)
![TypeScript](https://img.shields.io/badge/TypeScript-Strict-blue)

## Live Demo

<div className='dropdown-demo-container'>
<LiveCode
  code={`<DropdownMenuRoot>
        <DropdownMenuTrigger className='dropdown-trigger'>
          Options
          <svg
            className='dropdown-svg'
            xmlns='http://www.w3.org/2000/svg'
            width='20'
            height='20'
            viewBox='0 0 20 20'
            fill='none'
          >
            <path d='M13.2292 7.5001L9.99583 10.7334L6.7625 7.5001C6.4375 7.1751 5.9125 7.1751 5.5875 7.5001C5.2625 7.8251 5.2625 8.3501 5.5875 8.6751L9.4125 12.5001C9.7375 12.8251 10.2625 12.8251 10.5875 12.5001L14.4125 8.6751C14.7375 8.3501 14.7375 7.8251 14.4125 7.5001C14.0875 7.18343 13.5542 7.1751 13.2292 7.5001Z' />
          </svg>
        </DropdownMenuTrigger>
        <DropdownMenuContent className='dropdown-content' align='end' sideOffset={4}>
          <DropdownMenuItem className='dropdown-item'>
            <span className='dropdown-item-avatar'></span>
            Alice
          </DropdownMenuItem>
          <DropdownMenuItem className='dropdown-item'>
            <span className='dropdown-item-avatar'></span>
            Bob
          </DropdownMenuItem>
          <DropdownMenuItem className='dropdown-item'>
            <span className='dropdown-item-avatar'></span>
            Charlie
          </DropdownMenuItem>
          <DropdownMenuItem className='dropdown-item'>
            <span className='dropdown-item-avatar'></span>
            Diana
          </DropdownMenuItem>
        </DropdownMenuContent>
      </DropdownMenuRoot>`}
    cssCode={`.dropdown-trigger {
  margin-inline: auto;
  width: 100px;
  display: flex;
  padding: 10px;
  align-items: center;
  justify-content: space-between;
  border-radius: 5px;
  border: 1px solid #e1e4ea;
  background: rgba(0, 0, 0, 0.16);
}

[data-theme='dark'] .dropdown-trigger {
border-color: #2b303b;
background: rgba(255, 255, 255, 0.16);
}

.dropdown-svg {
width: 20px;
height: 20px;
fill: #222530;
}

[data-theme='dark'] .dropdown-svg {
fill: #f2f5f8;
}

.dropdown-content {
padding: 8px;
border-radius: 11px;
border: 0.684px solid #e1e4ea;
background: #ddd;
box-shadow:
0 1.369px 1.369px -0.684px rgba(82, 88, 102, 0.04),
0 4.106px 8.213px -1.369px rgba(82, 88, 102, 0.04),
0 5.475px 13.688px 0 rgba(82, 88, 102, 0.04);
display: flex;
flex-direction: column;
gap: 4px;
}

[data-theme='dark'] .dropdown-content {
border: 0.684px solid #2b303b;
background: #222;
box-shadow:
0 2px 2px -1px rgba(82, 88, 102, 0.04),
0 6px 12px -2px rgba(82, 88, 102, 0.04),
0 8px 20px 0 rgba(82, 88, 102, 0.04);
}

.dropdown-item {
padding: 5.475px 8px;
display: flex;
align-items: center;
gap: 4px;
border-radius: 4.977px;
background: rgba(255, 255, 255, 0.48);
width: 125px;
}

[data-theme='dark'] .dropdown-item {
background: rgba(0, 0, 0, 0.48);
}

.dropdown-item-avatar {
border-radius: 50%;
display: block;
width: 13.688px;
height: 13.688px;
background-color: #474747;
}`}
/>

</div>

## Features

- ✅ Full keyboard navigation with arrow keys
- ✅ Typeahead search for quick item selection
- ✅ Nested submenu support with directional awareness
- ✅ Checkbox and radio item variants
- ✅ Controlled and uncontrolled state management
- ✅ Automatic positioning with collision detection
- ✅ Modal and non-modal modes
- ✅ Focus management and restoration
- ✅ Customizable close behavior per item type
- ✅ Polymorphic component API

## Installation

```tsx
// Compound component (recommended)
import {
  DropdownMenuRoot,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuCheckboxItem,
  DropdownMenuRadioGroup,
  DropdownMenuRadioItem,
  DropdownMenuSeparator,
  DropdownMenuLabel,
  DropdownMenuGroup,
  DropdownMenuSub,
  DropdownMenuSubTrigger,
  DropdownMenuSubContent,
} from '@turkish-technology/spar';
```

## Anatomy

The DropdownMenu component is built as a compound component with multiple parts:

<AnatomyViewer
  parts={[
    { name: 'root', label: 'Root', description: 'Container managing state and context' },
    { name: 'trigger', label: 'Trigger', description: 'Button that toggles menu visibility' },
    { name: 'content', label: 'Content', description: 'Menu container with positioning' },
    { name: 'item', label: 'Item', description: 'Individual selectable menu option' },
    { name: 'separator', label: 'Separator', description: 'Visual separator between sections' },
  ]}
>
  <DropdownMenuRoot data-spar-part='root'>
    <DropdownMenuTrigger
      data-spar-part='trigger'
      style={{
        padding: '8px 16px',
        border: '1px solid #ccc',
        borderRadius: '4px',
        background: 'white',
        cursor: 'pointer',
      }}
    >
      {' Open Menu '}
    </DropdownMenuTrigger>
    <DropdownMenuContent
      data-spar-part='content'
      style={{
        border: '1px solid #ccc',
        borderRadius: '4px',
        background: 'white',
        padding: '4px',
        minWidth: '180px',
      }}
    >
      <DropdownMenuItem data-spar-part='item' style={{ padding: '8px 12px', cursor: 'pointer' }}>
        {' New File '}
      </DropdownMenuItem>
      <DropdownMenuItem data-spar-part='item' style={{ padding: '8px 12px', cursor: 'pointer' }}>
        {' Save '}
      </DropdownMenuItem>
      <DropdownMenuSeparator
        data-spar-part='separator'
        style={{ height: '1px', background: '#e0e0e0', margin: '4px 0' }}
      />
      <DropdownMenuItem data-spar-part='item' style={{ padding: '8px 12px', cursor: 'pointer' }}>
        {' Exit '}
      </DropdownMenuItem>
    </DropdownMenuContent>
  </DropdownMenuRoot>
</AnatomyViewer>

### Structure

```tsx
<DropdownMenuRoot>
  <DropdownMenuTrigger />
  <DropdownMenuContent>
    <DropdownMenuLabel />
    <DropdownMenuItem />
    <DropdownMenuGroup>
      <DropdownMenuItem />
    </DropdownMenuGroup>
    <DropdownMenuCheckboxItem />
    <DropdownMenuRadioGroup>
      <DropdownMenuRadioItem />
    </DropdownMenuRadioGroup>
    <DropdownMenuSub>
      <DropdownMenuSubTrigger />
      <DropdownMenuSubContent>
        <DropdownMenuItem />
      </DropdownMenuSubContent>
    </DropdownMenuSub>
    <DropdownMenuSeparator />
  </DropdownMenuContent>
</DropdownMenuRoot>
```

**Alternative: Tree-shakeable named imports**

```tsx
import {
  DropdownMenuRoot,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
} from '@turkish-technology/spar';

<DropdownMenuRoot>
  <DropdownMenuTrigger />
  <DropdownMenuContent>
    <DropdownMenuItem />
  </DropdownMenuContent>
</DropdownMenuRoot>;
```

**Data Flow:**

- `DropdownMenuRoot` manages open/close state and provides context
- `DropdownMenuTrigger` controls menu visibility and manages focus
- `DropdownMenuContent` handles positioning, keyboard navigation, and item collection
- Menu items register themselves for keyboard navigation and typeahead
- Submenus inherit parent context and manage their own open state

## Code Examples

### Basic Dropdown Menu

A simple dropdown menu with standard items.

```tsx
function BasicMenu() {
  return (
    <DropdownMenuRoot>
      <DropdownMenuTrigger>Actions</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem onSelect={() => console.log('Edit')}>Edit</DropdownMenuItem>
        <DropdownMenuItem onSelect={() => console.log('Duplicate')}>Duplicate</DropdownMenuItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem onSelect={() => console.log('Delete')}>Delete</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Controlled Menu State

Control the menu's open state externally.

```tsx
function ControlledMenu() {
  const [isOpen, setIsOpen] = React.useState(false);

  return (
    <>
      <p>Menu is {isOpen ? 'open' : 'closed'}</p>
      <DropdownMenuRoot open={isOpen} onOpenChange={setIsOpen}>
        <DropdownMenuTrigger>Options</DropdownMenuTrigger>
        <DropdownMenuContent>
          <DropdownMenuItem>Option 1</DropdownMenuItem>
          <DropdownMenuItem>Option 2</DropdownMenuItem>
        </DropdownMenuContent>
      </DropdownMenuRoot>
    </>
  );
}
```

### Menu with Checkbox Items

Checkbox items for toggleable options.

```tsx
function MenuWithCheckboxes() {
  const [showBookmarks, setShowBookmarks] = React.useState(true);
  const [showHistory, setShowHistory] = React.useState(false);

  return (
    <DropdownMenuRoot>
      <DropdownMenuTrigger>View</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuLabel>Panels</DropdownMenuLabel>
        <DropdownMenuCheckboxItem checked={showBookmarks} onCheckedChange={setShowBookmarks}>
          Show Bookmarks
        </DropdownMenuCheckboxItem>
        <DropdownMenuCheckboxItem checked={showHistory} onCheckedChange={setShowHistory}>
          Show History
        </DropdownMenuCheckboxItem>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Menu with Radio Group

Radio groups for mutually exclusive selections.

```tsx
function MenuWithRadioGroup() {
  const [textAlign, setTextAlign] = React.useState('left');

  return (
    <DropdownMenuRoot>
      <DropdownMenuTrigger>Text Align</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuRadioGroup value={textAlign} onValueChange={setTextAlign}>
          <DropdownMenuRadioItem value='left'>Align Left</DropdownMenuRadioItem>
          <DropdownMenuRadioItem value='center'>Align Center</DropdownMenuRadioItem>
          <DropdownMenuRadioItem value='right'>Align Right</DropdownMenuRadioItem>
        </DropdownMenuRadioGroup>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Nested Submenus

Create nested menus with directional navigation.

```tsx
function NestedMenu() {
  return (
    <DropdownMenuRoot>
      <DropdownMenuTrigger>File</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem>New File</DropdownMenuItem>
        <DropdownMenuSub>
          <DropdownMenuSubTrigger>Open Recent</DropdownMenuSubTrigger>
          <DropdownMenuSubContent>
            <DropdownMenuItem>document.txt</DropdownMenuItem>
            <DropdownMenuItem>project.json</DropdownMenuItem>
            <DropdownMenuItem>notes.md</DropdownMenuItem>
          </DropdownMenuSubContent>
        </DropdownMenuSub>
        <DropdownMenuSeparator />
        <DropdownMenuItem>Save</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Custom Positioning

Control menu positioning and collision behavior.

```tsx
function CustomPositionMenu() {
  return (
    <DropdownMenuRoot>
      <DropdownMenuTrigger>Options</DropdownMenuTrigger>
      <DropdownMenuContent
        side='right'
        align='start'
        sideOffset={5}
        alignOffset={10}
        avoidCollisions={true}
      >
        <DropdownMenuItem>Option 1</DropdownMenuItem>
        <DropdownMenuItem>Option 2</DropdownMenuItem>
        <DropdownMenuItem>Option 3</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Grouped Menu Items

Group related items together.

```tsx
function GroupedMenu() {
  return (
    <DropdownMenuRoot>
      <DropdownMenuTrigger>Edit</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuGroup>
          <DropdownMenuLabel>Clipboard</DropdownMenuLabel>
          <DropdownMenuItem>Cut</DropdownMenuItem>
          <DropdownMenuItem>Copy</DropdownMenuItem>
          <DropdownMenuItem>Paste</DropdownMenuItem>
        </DropdownMenuGroup>
        <DropdownMenuSeparator />
        <DropdownMenuGroup>
          <DropdownMenuLabel>Transform</DropdownMenuLabel>
          <DropdownMenuItem>Uppercase</DropdownMenuItem>
          <DropdownMenuItem>Lowercase</DropdownMenuItem>
        </DropdownMenuGroup>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Disabled Items

Disable specific menu items.

```tsx
function MenuWithDisabledItems() {
  const hasSelection = false;

  return (
    <DropdownMenuRoot>
      <DropdownMenuTrigger>Edit</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem disabled={!hasSelection}>Cut</DropdownMenuItem>
        <DropdownMenuItem disabled={!hasSelection}>Copy</DropdownMenuItem>
        <DropdownMenuItem>Paste</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Non-Modal Menu

Allow focus to leave the menu without closing.

```tsx
function NonModalMenu() {
  return (
    <DropdownMenuRoot modal={false}>
      <DropdownMenuTrigger>Settings</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem>Preferences</DropdownMenuItem>
        <DropdownMenuItem>Extensions</DropdownMenuItem>
        <DropdownMenuItem>Keyboard Shortcuts</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

### Custom Close Behavior

Control when the menu closes on selection.

```tsx
function CustomCloseBehavior() {
  return (
    <DropdownMenuRoot closeOnSelect={false}>
      <DropdownMenuTrigger>Multi-Select</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuCheckboxItem checked={false}>Option 1</DropdownMenuCheckboxItem>
        <DropdownMenuCheckboxItem checked={false}>Option 2</DropdownMenuCheckboxItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem onSelect={(e) => e.preventDefault()}>Keep Open</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenuRoot>
  );
}
```

## API Reference

### DropdownMenuRoot

The root container that manages the dropdown menu state and provides context to all child components.

**Props**

| Name             | Type                      | Default     | Description                                                                                     |
| ---------------- | ------------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| `open?`          | `boolean`                 | `undefined` | Controlled open state                                                                           |
| `defaultOpen?`   | `boolean`                 | `false`     | Initial open state (uncontrolled)                                                               |
| `onOpenChange?`  | `(open: boolean) => void` | `undefined` | Callback when open state changes                                                                |
| `modal?`         | `boolean`                 | `true`      | Whether menu traps focus (modal) or allows focus to leave                                       |
| `disabled?`      | `boolean`                 | `false`     | Disables all dropdown menu triggers (prevents opening)                                          |
| `dir?`           | `'ltr' \| 'rtl'`          | `'ltr'`     | Reading direction for submenu positioning                                                       |
| `closeOnSelect?` | `boolean \| 'auto'`       | `'auto'`    | Whether to close menu on item selection. 'auto' closes for items, stays open for checkbox/radio |
| `children?`      | `ReactNode`               | `undefined` | Menu trigger and content                                                                        |

**Events**

| Name           | Parameters        | Description               | When Triggered                                |
| -------------- | ----------------- | ------------------------- | --------------------------------------------- |
| `onOpenChange` | `(open: boolean)` | Open state change handler | User interaction or programmatic state change |

**HTML Attributes**

_(No specific HTML attributes - Root is a context provider with no rendered element)_

### DropdownMenuTrigger

The button element that toggles the dropdown menu visibility. Extends all `Button` component props including polymorphic rendering via the `as` prop.

**Props**

| Name               | Type                                                                  | Default     | Description                                         |
| ------------------ | --------------------------------------------------------------------- | ----------- | --------------------------------------------------- |
| `as?`              | `ElementType`                                                         | `"button"`  | Element type to render as                           |
| `children`         | `ReactNode \| ((state: DropdownMenuTriggerRenderProps) => ReactNode)` | `undefined` | Trigger content or render props function            |
| `disabled?`        | `boolean`                                                             | `false`     | Whether the trigger is disabled                     |
| `autoFocus?` | `boolean`                                                             | `false`     | Whether the button should receive focus on mount    |
| `isLoading?`       | `boolean`                                                             | `false`     | Loading state with screen reader support            |
| `isPressed?`       | `boolean`                                                             | `undefined` | Toggle state - creates a toggle button when defined |
| `onPressedChange?` | `(pressed: boolean) => void`                                          | `undefined` | Callback fired when toggle state changes            |
| `ref?`             | `Ref<HTMLButtonElement \| null>`                                      | `undefined` | Forwarded ref to trigger element                    |
| `onClick?`         | `(event: MouseEvent) => void`                                         | `undefined` | Click event handler                                 |
| `onKeyDown?`       | `(event: KeyboardEvent) => void`                                      | `undefined` | Keyboard event handler                              |
| `...props`         | `ButtonProps`                                                         | -           | All standard Button component attributes            |

**Render Props (DropdownMenuTriggerRenderProps)**

| Property   | Type         | Description                          |
| ---------- | ------------ | ------------------------------------ |
| `isOpen`   | `boolean`    | Whether the dropdown menu is open    |
| `disabled` | `boolean`    | Whether the trigger is disabled      |
| `open`     | `() => void` | Function to open the dropdown menu   |
| `close`    | `() => void` | Function to close the dropdown menu  |
| `toggle`   | `() => void` | Function to toggle the dropdown menu |

**Events**

| Name        | Parameters               | Description            | When Triggered                 |
| ----------- | ------------------------ | ---------------------- | ------------------------------ |
| `onClick`   | `(event: MouseEvent)`    | Mouse click handler    | User clicks trigger            |
| `onKeyDown` | `(event: KeyboardEvent)` | Keyboard event handler | User presses key while focused |

**HTML Attributes**

| Attribute       | Value                | Purpose                   |
| --------------- | -------------------- | ------------------------- |
| `data-state`    | `'open' \| 'closed'` | Current state for styling |
| `data-disabled` | `''`                 | Present when disabled     |

### DropdownMenuContent

The container for menu items with positioning, keyboard navigation, and typeahead support.

**Props**

| Name                    | Type                                     | Default     | Description                                           |
| ----------------------- | ---------------------------------------- | ----------- | ----------------------------------------------------- |
| `as?`                   | `ElementType`                          | `'div'`     | Polymorphic element type to render                    |
| `side?`                 | `'top' \| 'right' \| 'bottom' \| 'left'` | `'bottom'`  | Preferred side for positioning                        |
| `align?`                | `'start' \| 'center' \| 'end'`           | `'start'`   | Alignment relative to trigger                         |
| `sideOffset?`           | `number`                                 | `8`         | Distance in pixels from trigger                       |
| `alignOffset?`          | `number`                                 | `0`         | Offset along alignment axis                           |
| `avoidCollisions?`      | `boolean`                                | `true`      | Automatically adjust position to avoid viewport edges |
| `collisionBoundary?`    | `Element \| Element[] \| null`           | `null`      | Boundary element(s) for collision detection           |
| `collisionPadding?`     | `number`                                 | `8`         | Padding from boundary edges in pixels                 |
| `loop?`                 | `boolean`                                | `false`     | Whether keyboard navigation loops through items       |
| `onEscapeKeyDown?`      | `(event: KeyboardEvent) => void`         | `undefined` | Escape key handler                                    |
| `onPointerDownOutside?` | `(event: PointerEvent) => void`          | `undefined` | Outside click handler                                 |
| `onFocusOutside?`       | `(event: FocusEvent) => void`            | `undefined` | Outside focus handler                                 |
| `onKeyDown?`            | `(event: KeyboardEvent) => void`         | `undefined` | Keyboard event handler                                |
| `ref?`                  | `Ref<HTMLElement \| null>`               | `undefined` | Forwarded ref to content element                      |
| `children?`             | `ReactNode`                              | `undefined` | Menu items and groups                                 |

**Events**

| Name                   | Parameters               | Description         | When Triggered                             |
| ---------------------- | ------------------------ | ------------------- | ------------------------------------------ |
| `onEscapeKeyDown`      | `(event: KeyboardEvent)` | Escape key pressed  | User presses Escape while menu is open     |
| `onPointerDownOutside` | `(event: PointerEvent)`  | Click outside menu  | User clicks outside menu bounds            |
| `onFocusOutside`       | `(event: FocusEvent)`    | Focus leaves menu   | Focus moves outside menu (non-modal mode)  |
| `onKeyDown`            | `(event: KeyboardEvent)` | Key pressed in menu | User presses any key while menu is focused |

**HTML Attributes**

| Attribute    | Value    | Purpose                        |
| ------------ | -------- | ------------------------------ |
| `data-state` | `'open'` | Indicates open state           |
| `data-side`  | Side     | Current placement side         |
| `data-align` | Align    | Current alignment              |
| `tabIndex`   | `-1`     | Manages focus programmatically |

### DropdownMenuItem

A selectable menu item.

**Props**

| Name         | Type                              | Default     | Description                                                           |
| ------------ | --------------------------------- | ----------- | --------------------------------------------------------------------- |
| `as?`        | `ElementType`                   | `'div'`     | Polymorphic element type to render                                    |
| `disabled?`  | `boolean`                         | `false`     | Whether item is disabled                                              |
| `onSelect?`  | `(event: SyntheticEvent) => void` | `undefined` | Selection handler                                                     |
| `textValue?` | `string`                          | `undefined` | Text value for typeahead (auto-detected from content if not provided) |
| `ref?`       | `Ref<HTMLElement \| null>`        | `undefined` | Forwarded ref to item element                                         |
| `children?`  | `ReactNode`                       | `undefined` | Item content                                                          |

**Events**

| Name            | Parameters                | Description         | When Triggered                              |
| --------------- | ------------------------- | ------------------- | ------------------------------------------- |
| `onSelect`      | `(event: SyntheticEvent)` | Item selected       | User clicks or activates item with keyboard |
| `onClick`       | `(event: MouseEvent)`     | Mouse click         | User clicks item                            |
| `onKeyDown`     | `(event: KeyboardEvent)`  | Key pressed         | User presses key while item focused         |
| `onPointerMove` | `(event: PointerEvent)`   | Pointer movement    | Pointer moves over item                     |
| `onFocus`       | `(event: FocusEvent)`     | Item receives focus | Item gains focus                            |

**HTML Attributes**

| Attribute          | Value     | Purpose                                                    |
| ------------------ | --------- | ---------------------------------------------------------- |
| `tabIndex`         | `0 \| -1` | Controls keyboard focus (0 when highlighted, -1 otherwise) |
| `data-disabled`    | `''`      | Present when disabled                                      |
| `data-highlighted` | `''`      | Present when highlighted                                   |

### DropdownMenuCheckboxItem

A menu item with checkbox functionality for toggleable options.

**Props**

| Name               | Type                              | Default     | Description                        |
| ------------------ | --------------------------------- | ----------- | ---------------------------------- |
| `as?`              | `ElementType`                   | `'div'`     | Polymorphic element type to render |
| `checked?`         | `boolean \| 'indeterminate'`      | `false`     | Checked state                      |
| `onCheckedChange?` | `(checked: boolean) => void`      | `undefined` | Checked state change handler       |
| `disabled?`        | `boolean`                         | `false`     | Whether item is disabled           |
| `onSelect?`        | `(event: SyntheticEvent) => void` | `undefined` | Selection handler                  |
| `textValue?`       | `string`                          | `undefined` | Text value for typeahead           |
| `ref?`             | `Ref<HTMLElement \| null>`        | `undefined` | Forwarded ref to item element      |
| `children?`        | `ReactNode`                       | `undefined` | Item content                       |

**Events**

| Name              | Parameters                | Description           | When Triggered        |
| ----------------- | ------------------------- | --------------------- | --------------------- |
| `onCheckedChange` | `(checked: boolean)`      | Checked state changes | User toggles checkbox |
| `onSelect`        | `(event: SyntheticEvent)` | Item selected         | User activates item   |

**HTML Attributes**

| Attribute      | Value                                  | Purpose                           |
| -------------- | -------------------------------------- | --------------------------------- |
| `data-checked` | `'true' \| 'false' \| 'indeterminate'` | Current checked state for styling |
| `tabIndex`     | `0 \| -1`                              | Controls keyboard focus           |

### DropdownMenuRadioGroup

Container for radio menu items to manage mutually exclusive selection.

**Props**

| Name             | Type                      | Default     | Description              |
| ---------------- | ------------------------- | ----------- | ------------------------ |
| `value?`         | `string`                  | `undefined` | Currently selected value |
| `onValueChange?` | `(value: string) => void` | `undefined` | Selection change handler |
| `children?`      | `ReactNode`               | `undefined` | RadioItem components     |

**Events**

| Name            | Parameters        | Description            | When Triggered            |
| --------------- | ----------------- | ---------------------- | ------------------------- |
| `onValueChange` | `(value: string)` | Selected value changes | User selects a radio item |

**HTML Attributes**

_(No specific HTML attributes - context provider for radio state)_

### DropdownMenuRadioItem

A radio menu item for mutually exclusive selections within a RadioGroup.

**Props**

| Name         | Type                              | Default      | Description                        |
| ------------ | --------------------------------- | ------------ | ---------------------------------- |
| `value`      | `string`                          | **required** | Unique value for this radio item   |
| `as?`        | `ElementType`                   | `'div'`      | Polymorphic element type to render |
| `disabled?`  | `boolean`                         | `false`      | Whether item is disabled           |
| `onSelect?`  | `(event: SyntheticEvent) => void` | `undefined`  | Selection handler                  |
| `textValue?` | `string`                          | `undefined`  | Text value for typeahead           |
| `ref?`       | `Ref<HTMLElement \| null>`        | `undefined`  | Forwarded ref to item element      |
| `children?`  | `ReactNode`                       | `undefined`  | Item content                       |

**Events**

| Name       | Parameters                | Description   | When Triggered      |
| ---------- | ------------------------- | ------------- | ------------------- |
| `onSelect` | `(event: SyntheticEvent)` | Item selected | User activates item |

**HTML Attributes**

| Attribute      | Value               | Purpose                           |
| -------------- | ------------------- | --------------------------------- |
| `data-checked` | `'true' \| 'false'` | Current checked state for styling |
| `tabIndex`     | `0 \| -1`           | Controls keyboard focus           |

### DropdownMenuSeparator

A visual separator between menu sections.

**Props**

| Name   | Type                       | Default     | Description                        |
| ------ | -------------------------- | ----------- | ---------------------------------- |
| `as?`  | `ElementType`            | `'div'`     | Polymorphic element type to render |
| `ref?` | `Ref<HTMLElement \| null>` | `undefined` | Forwarded ref to separator element |

**Events**

| Name | Parameters | Description           | When Triggered               |
| ---- | ---------- | --------------------- | ---------------------------- |
| N/A  | N/A        | No interactive events | Separator is non-interactive |

**HTML Attributes**

| Attribute          | Value          | Purpose               |
| ------------------ | -------------- | --------------------- |
| `data-orientation` | `'horizontal'` | Indicates orientation |

### DropdownMenuLabel

A non-interactive label for menu sections.

**Props**

| Name        | Type                       | Default     | Description                        |
| ----------- | -------------------------- | ----------- | ---------------------------------- |
| `as?`       | `ElementType`            | `'div'`     | Polymorphic element type to render |
| `ref?`      | `Ref<HTMLElement \| null>` | `undefined` | Forwarded ref to label element     |
| `children?` | `ReactNode`                | `undefined` | Label content                      |

**Events**

| Name | Parameters | Description           | When Triggered           |
| ---- | ---------- | --------------------- | ------------------------ |
| N/A  | N/A        | No interactive events | Label is non-interactive |

**HTML Attributes**

_(No specific HTML attributes - Label is non-interactive)_

### DropdownMenuGroup

A logical grouping container for related menu items.

**Props**

| Name        | Type                       | Default     | Description                        |
| ----------- | -------------------------- | ----------- | ---------------------------------- |
| `as?`       | `ElementType`            | `'div'`     | Polymorphic element type to render |
| `ref?`      | `Ref<HTMLElement \| null>` | `undefined` | Forwarded ref to group element     |
| `children?` | `ReactNode`                | `undefined` | Menu items or labels               |

**Events**

| Name | Parameters | Description           | When Triggered                     |
| ---- | ---------- | --------------------- | ---------------------------------- |
| N/A  | N/A        | No interactive events | Group is non-interactive container |

**HTML Attributes**

_(No specific HTML attributes - groups related menu items)_

### DropdownMenuSub

Container for nested submenu functionality.

**Props**

| Name            | Type                      | Default     | Description                               |
| --------------- | ------------------------- | ----------- | ----------------------------------------- |
| `open?`         | `boolean`                 | `undefined` | Controlled submenu open state             |
| `defaultOpen?`  | `boolean`                 | `false`     | Initial submenu open state (uncontrolled) |
| `onOpenChange?` | `(open: boolean) => void` | `undefined` | Callback when submenu open state changes  |
| `children?`     | `ReactNode`               | `undefined` | SubTrigger and SubContent components      |

**Events**

| Name           | Parameters        | Description                | When Triggered            |
| -------------- | ----------------- | -------------------------- | ------------------------- |
| `onOpenChange` | `(open: boolean)` | Submenu open state changes | User opens/closes submenu |

**HTML Attributes**

_(No specific HTML attributes - Sub is a context provider with no rendered element)_

### DropdownMenuSubTrigger

A menu item that triggers a nested submenu.

**Props**

| Name         | Type                              | Default     | Description                         |
| ------------ | --------------------------------- | ----------- | ----------------------------------- |
| `as?`        | `ElementType`                   | `'div'`     | Polymorphic element type to render  |
| `disabled?`  | `boolean`                         | `false`     | Whether subtrigger is disabled      |
| `onSelect?`  | `(event: SyntheticEvent) => void` | `undefined` | Selection handler                   |
| `textValue?` | `string`                          | `undefined` | Text value for typeahead            |
| `ref?`       | `Ref<HTMLElement \| null>`        | `undefined` | Forwarded ref to subtrigger element |
| `children?`  | `ReactNode`                       | `undefined` | Subtrigger content                  |

**Events**

| Name            | Parameters               | Description                | When Triggered                            |
| --------------- | ------------------------ | -------------------------- | ----------------------------------------- |
| `onPointerMove` | `(event: PointerEvent)`  | Pointer moves over trigger | Opens submenu on hover                    |
| `onKeyDown`     | `(event: KeyboardEvent)` | Key pressed                | Handles arrow keys for submenu navigation |
| `onClick`       | `(event: MouseEvent)`    | Mouse click                | Opens submenu                             |

**HTML Attributes**

| Attribute    | Value                | Purpose                   |
| ------------ | -------------------- | ------------------------- |
| `data-state` | `'open' \| 'closed'` | Current state for styling |
| `tabIndex`   | `0 \| -1`            | Controls keyboard focus   |

### DropdownMenuSubContent

The content container for a nested submenu, inheriting most behavior from DropdownMenuContent.

**Props**

Inherits all props from `DropdownMenuContent`. Default `side` is `'right'` instead of `'bottom'`.

| Name                | Type                                     | Default   | Description                                                      |
| ------------------- | ---------------------------------------- | --------- | ---------------------------------------------------------------- |
| (All Content props) | -                                        | -         | Same as DropdownMenuContent                                     |
| `side?`             | `'top' \| 'right' \| 'bottom' \| 'left'` | `'right'` | Preferred side for positioning (auto-determined based on parent) |

**Events**

Same as `DropdownMenuContent`.

**HTML Attributes**

Same as `DropdownMenuContent`.

## Keyboard Interactions

Complete keyboard behavior for the DropdownMenu component:

| Key              | Action                                   | Context               | Notes                                                                    |
| ---------------- | ---------------------------------------- | --------------------- | ------------------------------------------------------------------------ |
| `Space`          | Opens menu / Activates item              | Trigger / Menu item   | Opens menu from trigger, activates focused item                          |
| `Enter`          | Opens menu / Activates item              | Trigger / Menu item   | Opens menu from trigger, activates focused item                          |
| `ArrowDown`      | Opens menu focusing first / Next item    | Trigger / Menu        | From trigger opens and focuses first item, in menu moves to next item    |
| `ArrowUp`        | Opens menu focusing last / Previous item | Trigger / Menu        | From trigger opens and focuses last item, in menu moves to previous item |
| `ArrowRight`     | Opens submenu                            | Submenu trigger (LTR) | Opens submenu focusing first item (reversed in RTL)                      |
| `ArrowLeft`      | Closes submenu                           | Submenu (LTR)         | Closes submenu and returns focus to trigger (reversed in RTL)            |
| `Escape`         | Closes menu                              | Menu                  | Closes menu and restores focus to trigger                                |
| `Tab`            | Navigate / Close                         | Menu                  | In modal mode, moves through items; in non-modal mode, closes menu       |
| `Shift + Tab`    | Navigate backward                        | Menu (modal)          | Moves to previous item in modal mode                                     |
| `Home`           | First item                               | Menu                  | Moves focus to first item                                                |
| `End`            | Last item                                | Menu                  | Moves focus to last item                                                 |
| `Character keys` | Typeahead search                         | Menu                  | Focuses first item matching typed characters                             |

**Note**: All keyboard interactions are implemented with full accessibility support following WAI-ARIA menu design patterns.
