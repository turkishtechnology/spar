---
title: Tabs
description: A headless, accessible tabs component with full keyboard navigation and ARIA compliance
---

import React from 'react';
import LiveCode from '../../src/components/LiveCode';
import AnatomyViewer from '../../src/components/AnatomyViewer/AnatomyViewer';
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@turkish-technology/spar';

# Tabs

A headless, accessible tabs component that provides complete keyboard navigation, orientation support, and ARIA compliance for tabbed interfaces.

![Accessibility](https://img.shields.io/badge/Accessibility-WCAG%202.2%20AA-green)
![Tree-shakeable](https://img.shields.io/badge/Tree--shakeable-Yes-blue)
![TypeScript](https://img.shields.io/badge/TypeScript-Strict-blue)

## Live Demo

<LiveCode
  code={`<div className='tabs-demo-container'>
<Tabs defaultValue='trend' className='tabs'>
        <TabsList className='tabs-list'>
          <TabsTrigger value='recent' className='tab'>
            <span className='tab-label'>Recent</span>
          </TabsTrigger>
          <TabsTrigger value='popular' className='tab'>
            <span className='tab-label'>Popular</span>
          </TabsTrigger>
          <TabsTrigger value='trend' className='tab'>
            <span className='tab-label'>Trend</span>
          </TabsTrigger>
        </TabsList>
        <TabsContent value='trend'>
        </TabsContent>
</Tabs>
</div>`}
cssCode={`.tabs {
  height: 29.288px;
  padding: 0 11.715px;
  border-radius: 5.858px;
  border: 0.732px solid rgba(0, 0, 0, 0.16);
  background: rgba(0, 0, 0, 0.08);
}

[data-theme='dark'] .tabs {
border: 0.732px solid rgba(255, 255, 255, 0.16);
background: rgba(255, 255, 255, 0.12);
}

.tabs-list {
display: flex;
align-items: center;
gap: 5.858px;
}

.tab {
border: none;
background: transparent;
color: #717784;
font-size: 10.251px;
height: 100%;
}

[data-theme='dark'] .tab {
color: #99a0ae;
}

.tab[data-state='active'] {
color: #222530;
}

[data-theme='dark'] .tab[data-state='active'] {
color: #f2f5f8;
}

.tab-label {
display: flex;
align-items: center;
justify-content: center;
height: 100%;
}

.tab[data-state='active'] .tab-label {
border-bottom: 1.464px solid rgba(0, 0, 0, 0.72);
}

[data-theme='dark'] .tab[data-state='active'] .tab-label {
border-color: rgba(255, 255, 255, 0.72);
}
`}
/>

## Features

- ✅ Controlled and uncontrolled state management
- ✅ Horizontal and vertical orientation support
- ✅ Automatic and manual activation modes
- ✅ Loop navigation with arrow keys
- ✅ Lazy content rendering for performance
- ✅ Force mount option for always-rendered content
- ✅ Polymorphic rendering with `as` prop
- ✅ Render props pattern for TabsTrigger customization
- ✅ Disabled tab support
- ✅ RTL (right-to-left) text direction support

## Installation

```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@turkish-technology/spar';
```

## Anatomy

The Tabs component consists of multiple compound parts that work together:

<AnatomyViewer
  parts={[
    {
      name: 'root',
      label: 'Tabs (Root)',
      description: 'Root container that manages state and provides context',
    },
    {
      name: 'list',
      label: 'TabsList',
      description: 'Container for tab triggers with keyboard navigation',
    },
    {
      name: 'trigger',
      label: 'TabsTrigger',
      description: 'Clickable tab button that activates content',
    },
    {
      name: 'content',
      label: 'TabsContent',
      description: 'Content panel associated with each tab',
    },
  ]}
>
  <div style={{ padding: '20px', border: '1px solid #ccc', borderRadius: '4px' }}>
    <Tabs defaultValue='overview' data-spar-part='root'>
      <TabsList
        data-spar-part='list'
        style={{ display: 'flex', borderBottom: '1px solid #ddd', marginBottom: '16px' }}
      >
        <TabsTrigger
          value='overview'
          data-spar-part='trigger'
          style={{
            padding: '8px 16px',
            border: 'none',
            background: 'none',
            cursor: 'pointer',
            borderBottom: '2px solid transparent',
          }}
        >
          {' Overview '}
        </TabsTrigger>
        <TabsTrigger
          value='details'
          data-spar-part='trigger'
          style={{
            padding: '8px 16px',
            border: 'none',
            background: 'none',
            cursor: 'pointer',
            borderBottom: '2px solid transparent',
          }}
        >
          {' Details '}
        </TabsTrigger>
      </TabsList>
      <TabsContent value='overview' data-spar-part='content'>
        <h4>{' Overview Panel '}</h4>
        <p>{' This is the overview content panel. '}</p>
      </TabsContent>
      <TabsContent value='details' data-spar-part='content'>
        <h4>{' Details Panel '}</h4>
        <p>{' This is the details content panel. '}</p>
      </TabsContent>
    </Tabs>
  </div>
</AnatomyViewer>

```tsx
<Tabs>
  <TabsList>
    <TabsTrigger />
    <TabsTrigger />
  </TabsList>
  <TabsContent />
  <TabsContent />
</Tabs>
```

### Data Flow

1. **Tabs (Root)** manages the selected value state and provides context to all child components
2. **TabsList** handles keyboard navigation between triggers and focus management
3. **TabsTrigger** components register themselves with the root and handle activation
4. **TabsContent** components automatically show/hide based on the selected value

## Code Examples

### Controlled Tabs

```tsx
function ControlledTabs() {
  const [activeTab, setActiveTab] = useState('profile');

  return (
    <Tabs value={activeTab} onValueChange={setActiveTab}>
      <TabsList>
        <TabsTrigger value='profile'>Profile</TabsTrigger>
        <TabsTrigger value='settings'>Settings</TabsTrigger>
      </TabsList>
      <TabsContent value='profile'>Profile content here</TabsContent>
      <TabsContent value='settings'>Settings content here</TabsContent>
    </Tabs>
  );
}
```

### Vertical Orientation

```tsx
function VerticalTabs() {
  return (
    <Tabs orientation='vertical' defaultValue='tab1'>
      <TabsList>
        <TabsTrigger value='tab1'>First Tab</TabsTrigger>
        <TabsTrigger value='tab2'>Second Tab</TabsTrigger>
        <TabsTrigger value='tab3'>Third Tab</TabsTrigger>
      </TabsList>
      <TabsContent value='tab1'>First content</TabsContent>
      <TabsContent value='tab2'>Second content</TabsContent>
      <TabsContent value='tab3'>Third content</TabsContent>
    </Tabs>
  );
}
```

### Manual Activation Mode

```tsx
function ManualActivationTabs() {
  return (
    <Tabs activationMode='manual' defaultValue='home'>
      <TabsList>
        <TabsTrigger value='home'>Home</TabsTrigger>
        <TabsTrigger value='about'>About</TabsTrigger>
        <TabsTrigger value='contact'>Contact</TabsTrigger>
      </TabsList>
      <TabsContent value='home'>Home page content</TabsContent>
      <TabsContent value='about'>About page content</TabsContent>
      <TabsContent value='contact'>Contact page content</TabsContent>
    </Tabs>
  );
}
```

### Render Props Pattern

```tsx
function RenderPropsTabs() {
  return (
    <Tabs defaultValue='dashboard'>
      <TabsList>
        <TabsTrigger value='dashboard'>
          {({ isSelected, isFocused }) => (
            <span className={isSelected ? 'selected' : ''}>
              <DashboardIcon />
              Dashboard
              {isFocused && <span className='focus-indicator' />}
            </span>
          )}
        </TabsTrigger>
        <TabsTrigger value='analytics'>
          {({ isSelected }) => <span className={isSelected ? 'selected' : ''}>Analytics</span>}
        </TabsTrigger>
      </TabsList>
      <TabsContent value='dashboard'>Dashboard content</TabsContent>
      <TabsContent value='analytics'>Analytics content</TabsContent>
    </Tabs>
  );
}
```

### Force Mount Content

```tsx
function ForceMountTabs() {
  return (
    <Tabs defaultValue='lazy'>
      <TabsList>
        <TabsTrigger value='lazy'>Lazy Content</TabsTrigger>
        <TabsTrigger value='eager'>Always Mounted</TabsTrigger>
      </TabsList>
      <TabsContent value='lazy'>This content is only rendered when active</TabsContent>
      <TabsContent value='eager' forceMount>
        This content is always in the DOM (useful for forms, videos, etc.)
      </TabsContent>
    </Tabs>
  );
}
```

## API Reference

### Tabs (Root)

#### Props

| Name             | Type                                   | Default        | Description                                                   |
| ---------------- | -------------------------------------- | -------------- | ------------------------------------------------------------- |
| `value`          | `string \| undefined`                  | `undefined`    | Controlled selected tab value                                 |
| `defaultValue`   | `string \| undefined`                  | `undefined`    | Uncontrolled initial tab selection                            |
| `onValueChange`  | `(value: string) => void \| undefined` | `undefined`    | Callback when tab selection changes                           |
| `orientation`    | `'horizontal' \| 'vertical'`           | `'horizontal'` | Tabs orientation affecting keyboard navigation                |
| `dir`            | `'ltr' \| 'rtl'`                       | `'ltr'`        | Text direction for arrow key navigation                       |
| `activationMode` | `'automatic' \| 'manual'`              | `'automatic'`  | Whether tabs activate on focus or require explicit activation |
| `as`             | `ElementType`                        | `'div'`        | Polymorphic component type                                    |

#### Events

| Name            | Parameters      | Description           | When Triggered                             |
| --------------- | --------------- | --------------------- | ------------------------------------------ |
| `onValueChange` | `value: string` | Tab selection changed | User clicks tab or navigates with keyboard |

#### ARIA

| Attribute          | Value                    | Purpose                   |
| ------------------ | ------------------------ | ------------------------- |
| `data-orientation` | `horizontal \| vertical` | Indicates tab orientation |
| `data-dir`         | `ltr \| rtl`             | Indicates text direction  |

### TabsList

#### Props

| Name   | Type            | Default | Description                               |
| ------ | --------------- | ------- | ----------------------------------------- |
| `loop` | `boolean`       | `true`  | Whether arrow key navigation wraps around |
| `as`   | `ElementType` | `'div'` | Polymorphic component type                |

#### ARIA

| Attribute          | Value                    | Purpose                              |
| ------------------ | ------------------------ | ------------------------------------ |
| `role`             | `tablist`                | Identifies the element as a tab list |
| `aria-orientation` | `horizontal \| vertical` | Indicates tab list orientation       |
| `data-orientation` | `horizontal \| vertical` | Indicates tab list orientation       |

### TabsTrigger

Extends all `Button` component props including polymorphic rendering via the `as` prop.

#### Props

| Name               | Type                                                          | Default     | Description                                         |
| ------------------ | ------------------------------------------------------------- | ----------- | --------------------------------------------------- |
| `value`            | `string`                                                      | —           | Unique identifier for the tab                       |
| `as?`              | `ElementType`                                                 | `"button"`  | Element type to render as                           |
| `disabled?`        | `boolean`                                                     | `false`     | Disables this specific tab                          |
| `autoFocus?` | `boolean`                                                     | `false`     | Whether to focus this tab trigger on mount          |
| `isLoading?`       | `boolean`                                                     | `false`     | Loading state with screen reader support            |
| `isPressed?`       | `boolean`                                                     | `undefined` | Toggle state - creates a toggle button when defined |
| `onPressedChange?` | `(pressed: boolean) => void`                                  | `undefined` | Callback fired when toggle state changes            |
| `children`         | `ReactNode \| ((state: TabsTriggerRenderProps) => ReactNode)` | —           | Children content or render function                 |
| `...props`         | `ButtonProps`                                                 | -           | All standard Button component attributes            |

#### TabsTriggerRenderProps

| Property      | Type          | Description                                  |
| ------------- | ------------- | -------------------------------------------- |
| `isSelected`  | `boolean`     | Whether this tab is currently selected       |
| `select`      | `() => void`  | Function to select this tab programmatically |
| `disabled`    | `boolean`     | Whether this tab is disabled                 |
| `isFocused`   | `boolean`     | Whether this tab is currently focused        |
| `orientation` | `Orientation` | The tab's orientation                        |

#### Events

| Name      | Parameters          | Description | When Triggered                           |
| --------- | ------------------- | ----------- | ---------------------------------------- |
| `onClick` | `event: MouseEvent` | Tab clicked | User clicks the tab trigger              |
| `onFocus` | `event: FocusEvent` | Tab focused | Tab receives focus via keyboard or mouse |
| `onBlur`  | `event: FocusEvent` | Tab blurred | Tab loses focus                          |

#### ARIA

| Attribute          | Value                    | Purpose                         |
| ------------------ | ------------------------ | ------------------------------- |
| `role`             | `tab`                    | Identifies the element as a tab |
| `aria-selected`    | `true \| false`          | Indicates if tab is selected    |
| `aria-controls`    | `string`                 | References controlled tabpanel  |
| `aria-disabled`    | `true \| false`          | Indicates if tab is disabled    |
| `tabIndex`         | `0 \| -1`                | Manages focus in tab list       |
| `data-state`       | `active \| inactive`     | Indicates tab state             |
| `data-disabled`    | `'' \| undefined`        | Indicates disabled state        |
| `data-orientation` | `horizontal \| vertical` | Indicates tab orientation       |
| `data-value`       | `string`                 | Tab's unique value              |
| `data-autofocus`   | `'' \| undefined`        | Indicates auto-focus state      |

### TabsContent

#### Props

| Name         | Type            | Default | Description                                     |
| ------------ | --------------- | ------- | ----------------------------------------------- |
| `value`      | `string`        | —       | Unique identifier matching a TabsTrigger value  |
| `forceMount` | `boolean`       | `false` | Force content to remain mounted when not active |
| `as`         | `ElementType` | `'div'` | Polymorphic component type                      |

#### ARIA

| Attribute          | Value                    | Purpose                               |
| ------------------ | ------------------------ | ------------------------------------- |
| `role`             | `tabpanel`               | Identifies the element as a tab panel |
| `aria-labelledby`  | `string`                 | References controlling tab            |
| `tabIndex`         | `0`                      | Makes panel focusable                 |
| `hidden`           | `true \| false`          | Hides inactive panels                 |
| `data-state`       | `active \| inactive`     | Indicates panel state                 |
| `data-orientation` | `horizontal \| vertical` | Indicates tab orientation             |

## Keyboard Interactions

| Key                | Action                                        | Context                | Notes                            |
| ------------------ | --------------------------------------------- | ---------------------- | -------------------------------- |
| `Tab`              | Move focus to next/previous focusable element | Global                 | Standard browser behavior        |
| `Enter` or `Space` | Activate focused tab                          | Tab trigger focused    | Changes active panel             |
| `ArrowRight`       | Focus next tab                                | Horizontal orientation | Loops to first if `loop` is true |
| `ArrowLeft`        | Focus previous tab                            | Horizontal orientation | Loops to last if `loop` is true  |
| `ArrowDown`        | Focus next tab                                | Vertical orientation   | Loops to first if `loop` is true |
| `ArrowUp`          | Focus previous tab                            | Vertical orientation   | Loops to last if `loop` is true  |
| `Home`             | Focus first tab                               | Tab list focused       | Always moves to first tab        |
| `End`              | Focus last tab                                | Tab list focused       | Always moves to last tab         |

**Note**: In `automatic` activation mode, arrow keys both focus and activate tabs. In `manual` mode, arrow keys only move focus - you must press Enter or Space to activate.
