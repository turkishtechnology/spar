---
title: Dialog
description: Accessible modal and non-modal dialog component with focus management, escape key handling, click outside behavior, and complete ARIA support.
---

import React from 'react';
import LiveCode from '../../src/components/LiveCode';
import AnatomyViewer from '../../src/components/AnatomyViewer/AnatomyViewer';
import { Button, Dialog } from '@turkish-technology/spar';

# Dialog

A headless modal and non-modal dialog component that provides accessibility features and focus management.

![Accessibility Badge](https://img.shields.io/badge/Accessibility-✓-green) ![Tree-shakeable Badge](https://img.shields.io/badge/Tree%20shakeable-✓-green) ![TypeScript Badge](https://img.shields.io/badge/TypeScript-✓-blue)

## Live Demo

<div className='dialog-demo-container'>
  <LiveCode
    code={`function DialogDemo() {
  const [open, setOpen] = React.useState(false);
  return (
    <Dialog.Root className='dialog' open={open} onOpenChange={setOpen}>
      <Dialog.Trigger className='dialog-trigger'>
        Open Dialog
      </Dialog.Trigger>
      <Dialog.Portal className='dialog-portal'>
        <Dialog.Overlay className='dialog-overlay'/>
        <Dialog.Content className='dialog-content'>
          <Dialog.Title className='dialog-title'>
            Dialog Title
          </Dialog.Title>
          <Dialog.Description className='dialog-description'>
            If you delete this item, it will be permanently removed. Are you sure you want to delete
            it?
          </Dialog.Description>
          <div className='dialog-actions'>
            <Dialog.Close className='dialog-close'>
              Close
            </Dialog.Close>
            <Button className='dialog-continue'>
              Continue
            </Button>
          </div>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}`}
cssCode={`.dialog-overlay {
  position: fixed;
  inset: 0;
  background-color: rgba(0, 0, 0, 0.5);
}

.dialog-trigger {
  border-radius: 8px;
  border: none;
  padding: 12px 14px;
  font-weight: 400;

  /* Light mode styles */
  background-color: rgba(0, 0, 0, 0.08);
  color: #222530;
  font-size: 13px;
}

[data-theme='dark'] .dialog-trigger {
  /* Dark mode styles */
  background-color: rgba(255, 255, 255, 0.12);
  color: #f2f5f8;
}

.dialog-content {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border-radius: 16px;
  border: 0.625px solid #e1e4ea;
  background-color: #ebebeb;
  width: 480px;
  padding: 16px;
}

[data-theme='dark'] .dialog-content {
  border-color: #2b303b;
  background-color: #292929;
}

.dialog-title {
  color: #222530;
  font-size: 20px;
  margin-bottom: 8px;
}

[data-theme='dark'] .dialog-title {
  color: #f2f5f8;
}

.dialog-description {
  color: #717784;
  font-size: 14px;
  font-weight: 300;
  line-height: 20px;
}

[data-theme='dark'] .dialog-description {
  color: #99a0ae;
}

.dialog-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
}

.dialog-close {
  background-color: transparent;
  border: none;
  padding: 12px 14px;
  border-radius: 8px;
  cursor: pointer;
}

.dialog-continue {
  border-radius: 8px;
  border: none;
  padding: 12px 14px;
  font-weight: 400;

  /* Light mode styles */
  background-color: rgba(0, 0, 0, 0.08);
  color: #222530;
  font-size: 13px;
}

[data-theme='dark'] .dialog-continue {
  /* Dark mode styles */
  background-color: rgba(255, 255, 255, 0.12);
  color: #f2f5f8;
}`}
  />

</div>

## Features

- ✅ Modal and non-modal dialog support
- ✅ Controlled and uncontrolled state management
- ✅ Focus trapping and restoration
- ✅ Escape key handling
- ✅ Click outside to close
- ✅ Portal rendering for overlays
- ✅ Polymorphic components with `as` prop
- ✅ Custom focus management with initial and final focus
- ✅ Force mount for animation libraries
- ✅ Customizable ARIA roles and attributes

## Installation

```tsx
import { Dialog } from '@turkish-technology/spar';
```

## Anatomy

The Dialog component is built as a compound component with multiple parts:

<AnatomyViewer
  parts={[
    { name: 'root', label: 'Root', description: 'Container managing dialog state and context' },
    { name: 'trigger', label: 'Trigger', description: 'Button that opens the dialog' },
    { name: 'portal', label: 'Portal', description: 'Portal container for overlay and content' },
    { name: 'overlay', label: 'Overlay', description: 'Background overlay behind dialog content' },
    {
      name: 'content',
      label: 'Content',
      description: 'Main dialog container with focus management',
    },
    { name: 'title', label: 'Title', description: 'Dialog title for accessibility' },
    {
      name: 'description',
      label: 'Description',
      description: 'Dialog description for accessibility',
    },
    { name: 'close', label: 'Close', description: 'Button that closes the dialog' },
  ]}
>
  <Dialog.Root data-spar-part='root'>
    <Dialog.Trigger
      data-spar-part='trigger'
      style={{
        padding: '8px 16px',
        border: '1px solid #ccc',
        borderRadius: '4px',
        background: 'white',
        cursor: 'pointer',
      }}
    >
      {'Open Dialog'}
    </Dialog.Trigger>
    <Dialog.Portal data-spar-part='portal'>
      <Dialog.Overlay
        data-spar-part='overlay'
        style={{
          position: 'fixed',
          inset: 0,
          backgroundColor: 'rgba(0, 0, 0, 0.5)',
        }}
      />
      <Dialog.Content
        data-spar-part='content'
        style={{
          position: 'fixed',
          top: '50%',
          left: '50%',
          transform: 'translate(-50%, -50%)',
          backgroundColor: 'white',
          padding: '24px',
          borderRadius: '8px',
          border: '1px solid #ccc',
          minWidth: '300px',
        }}
      >
        <Dialog.Title
          data-spar-part='title'
          style={{ fontSize: '18px', fontWeight: 'bold', marginBottom: '8px' }}
        >
          {'Dialog Title'}
        </Dialog.Title>
        <Dialog.Description
          data-spar-part='description'
          style={{ color: '#666', marginBottom: '16px' }}
        >
          {'This is a basic dialog example with proper accessibility features.'}
        </Dialog.Description>
        <Dialog.Close
          data-spar-part='close'
          style={{
            padding: '8px 16px',
            border: '1px solid #ccc',
            borderRadius: '4px',
            background: 'white',
            cursor: 'pointer',
          }}
        >
          {'Close'}
        </Dialog.Close>
      </Dialog.Content>
    </Dialog.Portal>
  </Dialog.Root>
</AnatomyViewer>

### Structure

```tsx
<Dialog.Root>
  <Dialog.Trigger />
  <Dialog.Portal>
    <Dialog.Overlay />
    <Dialog.Content>
      <Dialog.Title />
      <Dialog.Description />
      <Dialog.Close />
    </Dialog.Content>
  </Dialog.Portal>
</Dialog.Root>
```

**Data Flow:**

- `Dialog.Root` manages open/close state and provides context to all child components
- `Dialog.Trigger` toggles the dialog state and maintains focus reference
- `Dialog.Portal` renders overlay and content outside the normal DOM tree
- `Dialog.Overlay` provides background overlay with click-outside-to-close behavior
- `Dialog.Content` handles focus management and keyboard interactions
- `Dialog.Title` and `Dialog.Description` provide accessible labeling
- `Dialog.Close` provides a way to close the dialog from within the content

## Code Examples

### Controlled State Management

Control the dialog's open state externally for integration with other components or form validation.

```tsx {2,4,12}
function ControlledDialog() {
  const [open, setOpen] = React.useState(false);

  const handleSave = () => {
    // Perform save operation
    console.log('Saving...');
    setOpen(false);
  };

  return (
    <Dialog.Root open={open} onOpenChange={setOpen}>
      <Dialog.Trigger>Edit Profile</Dialog.Trigger>
      <Dialog.Portal>
        <Dialog.Overlay />
        <Dialog.Content>
          <Dialog.Title>Edit Profile</Dialog.Title>
          <Dialog.Description>Make changes to your profile here.</Dialog.Description>

          <div style={{ marginTop: '16px' }}>
            <button onClick={handleSave}>Save Changes</button>
            <Dialog.Close>Cancel</Dialog.Close>
          </div>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}
```

### Uncontrolled with Default State

Use uncontrolled mode when you don't need to manage the dialog state externally.

```tsx {1}
function UncontrolledDialog() {
  return (
    <Dialog.Root defaultOpen={false}>
      <Dialog.Trigger>Open Settings</Dialog.Trigger>
      <Dialog.Portal>
        <Dialog.Overlay />
        <Dialog.Content>
          <Dialog.Title>Settings</Dialog.Title>
          <Dialog.Description>Configure your application settings.</Dialog.Description>
          <Dialog.Close>Done</Dialog.Close>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}
```

### Non-Modal Dialog

Create non-modal dialogs that don't block interaction with the underlying page.

```tsx {1,8-10}
function NonModalDialog() {
  return (
    <Dialog.Root modal={false}>
      <Dialog.Trigger>Open Info Panel</Dialog.Trigger>
      <Dialog.Portal>
        <Dialog.Content
          role='dialog'
          onEscapeKeyDown={(e) => {
            // Custom escape handling for non-modal
            console.log('Escape pressed in non-modal dialog');
          }}
        >
          <Dialog.Title>Information Panel</Dialog.Title>
          <Dialog.Description>
            This is a non-modal dialog. You can still interact with the page behind it.
          </Dialog.Description>
          <Dialog.Close>Close Panel</Dialog.Close>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}
```

### Custom Focus Management

Control initial focus and focus restoration for optimal user experience.

```tsx {6-8,10-12}
function CustomFocusDialog() {
  const [open, setOpen] = React.useState(false);
  const inputRef = React.useRef<HTMLInputElement>(null);

  return (
    <Dialog.Root open={open} onOpenChange={setOpen}>
      <Dialog.Trigger>Create Account</Dialog.Trigger>
      <Dialog.Portal>
        <Dialog.Overlay />
        <Dialog.Content
          initialFocus={() => inputRef.current!}
          onOpenAutoFocus={(event) => {
            // Custom logic before auto-focus
            console.log('Dialog is about to focus');
          }}
          onCloseAutoFocus={(event) => {
            // Custom logic before focus restoration
            console.log('Dialog is about to restore focus');
          }}
        >
          <Dialog.Title>Create New Account</Dialog.Title>
          <Dialog.Description>Enter your details to create an account.</Dialog.Description>

          <div style={{ margin: '16px 0' }}>
            <label>
              Username:
              <input ref={inputRef} type='text' />
            </label>
          </div>

          <div>
            <button type='submit'>Create</button>
            <Dialog.Close>Cancel</Dialog.Close>
          </div>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}
```

### Polymorphic Components

Use the `as` prop to render dialog parts as different HTML elements or custom components.

```tsx {2,7,12,17}
function PolymorphicDialog() {
  return (
    <Dialog.Root>
      <Dialog.Trigger as='div' role='button' tabIndex={0}>
        Custom Trigger Element
      </Dialog.Trigger>
      <Dialog.Portal>
        <Dialog.Overlay as='section' />
        <Dialog.Content>
          <Dialog.Title as='h1' level={1}>
            Main Dialog Title
          </Dialog.Title>
          <Dialog.Description as='div'>Custom description container</Dialog.Description>
          <Dialog.Close as='span' role='button' tabIndex={0}>
            Custom Close Element
          </Dialog.Close>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}
```

### Animation Support

Use `forceMount` for animation libraries that need to control mounting and unmounting.

```tsx {4,6-10}
function AnimatedDialog() {
  const [open, setOpen] = React.useState(false);

  return (
    <Dialog.Root open={open} onOpenChange={setOpen}>
      <Dialog.Trigger>Open Animated Dialog</Dialog.Trigger>
      <Dialog.Portal>
        <Dialog.Overlay
          forceMount
          style={{
            opacity: open ? 1 : 0,
            transition: 'opacity 0.2s ease',
          }}
        />
        <Dialog.Content
          forceMount
          style={{
            transform: open ? 'scale(1)' : 'scale(0.95)',
            opacity: open ? 1 : 0,
            transition: 'all 0.2s ease',
          }}
        >
          <Dialog.Title>Animated Dialog</Dialog.Title>
          <Dialog.Description>This dialog has custom animations.</Dialog.Description>
          <Dialog.Close>Close</Dialog.Close>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}
```

## API Reference

### Dialog.Root

The root container that manages dialog state and provides context to all child components.

**Props**

| Name            | Type                      | Default     | Description                                          |
| --------------- | ------------------------- | ----------- | ---------------------------------------------------- |
| `children`      | `ReactNode`               | -           | Dialog trigger and portal components                 |
| `open?`       | `boolean`                 | `undefined` | Controlled open state                                |
| `onOpenChange?` | `(open: boolean) => void` | `undefined` | Callback when open state changes                     |
| `defaultOpen?`  | `boolean`                 | `false`     | Initial open state for uncontrolled usage            |
| `modal?`        | `boolean`                 | `true`      | Whether dialog is modal (blocks interaction outside) |
| `disabled?`     | `boolean`                 | `false`     | Disables all dialog triggers (prevents opening)      |

**Events**

| Name           | Parameters        | Description        | When Triggered                          |
| -------------- | ----------------- | ------------------ | --------------------------------------- |
| `onOpenChange` | `(open: boolean)` | Open state changes | User interaction or programmatic change |

**HTML Attributes**

| Attribute       | Value                | Purpose              |
| --------------- | -------------------- | -------------------- |
| `data-state`    | `"open" \| "closed"` | Current dialog state |

### Dialog.Trigger

Interactive element that opens the dialog when activated.

**Props**

| Name          | Type                     | Default     | Description                      |
| ------------- | ------------------------ | ----------- | -------------------------------- |
| `children`    | `ReactNode \| ((state: DialogTriggerRenderProps) => ReactNode)` | - | Trigger content or render props function |
| `as?`         | `PolymorphicAs`            | `'button'`  | Polymorphic element type         |
| `disabled?` | `boolean`                | `false`     | Disables trigger interaction     |
| `ref?`        | `React.Ref<HTMLElement>` | `undefined` | Ref forwarded to trigger element |

**Render Props (DialogTriggerRenderProps)**

| Property   | Type         | Description                                     |
| ---------- | ------------ | ----------------------------------------------- |
| `isOpen`   | `boolean`    | Whether the dialog is currently visible         |
| `disabled` | `boolean`    | Whether the trigger is disabled                 |
| `open`     | `() => void` | Function to programmatically open the dialog    |
| `close`    | `() => void` | Function to programmatically close the dialog   |
| `toggle`   | `() => void` | Function to programmatically toggle the dialog  |

**Events**

| Name        | Parameters               | Description            | When Triggered       |
| ----------- | ------------------------ | ---------------------- | -------------------- |
| `onClick`   | `(event: MouseEvent)`    | Trigger clicked        | Mouse click          |
| `onKeyDown` | `(event: KeyboardEvent)` | Key pressed on trigger | Keyboard interaction |

**HTML Attributes**

| Attribute       | Value                 | Purpose                  |
| --------------- | --------------------- | ------------------------ |
| `data-state`    | `"open" \| "closed"`  | Current dialog state     |
| `data-disabled` | `string \| undefined` | Disabled state indicator |

### Dialog.Portal

Portal container that renders overlay and content outside the normal DOM tree.

**Props**

| Name         | Type                  | Default         | Description                |
| ------------ | --------------------- | --------------- | -------------------------- |
| `children`   | `ReactNode`           | -               | Dialog overlay and content |
| `container?` | `HTMLElement \| null` | `document.body` | Portal container element   |

**Events**

| Name | Parameters | Description | When Triggered |
| ---- | ---------- | ----------- | -------------- |
| -    | -          | No events   | -              |

**HTML Attributes**

_(No specific HTML attributes - portal container element)_

### Dialog.Overlay

Background overlay that appears behind dialog content and handles click-outside behavior.

**Props**

| Name          | Type                     | Default     | Description                           |
| ------------- | ------------------------ | ----------- | ------------------------------------- |
| `children?`   | `ReactNode`              | `undefined` | Overlay content (usually none)        |
| `as?`         | `PolymorphicAs`            | `'div'`     | Polymorphic element type              |
| `forceMount?` | `boolean`                | `false`     | Always render for animation libraries |
| `ref?`        | `React.Ref<HTMLElement>` | `undefined` | Ref forwarded to overlay element      |

**Events**

| Name      | Parameters            | Description     | When Triggered              |
| --------- | --------------------- | --------------- | --------------------------- |
| `onClick` | `(event: MouseEvent)` | Overlay clicked | Click on overlay background |

**HTML Attributes**

| Attribute    | Value                | Purpose              |
| ------------ | -------------------- | -------------------- |
| `data-state` | `"open" \| "closed"` | Current dialog state |

### Dialog.Content

Main dialog container that handles focus management and keyboard interactions.

**Props**

| Name            | Type                                 | Default     | Description                           |
| --------------- | ------------------------------------ | ----------- | ------------------------------------- |
| `children`      | `ReactNode`                          | -           | Dialog content                        |
| `as?`           | `PolymorphicAs`                        | `'div'`     | Polymorphic element type              |
| `role?`         | `React.AriaRole`                     | `'dialog'`  | ARIA role for dialog type             |
| `forceMount?`   | `boolean`                            | `false`     | Always render for animation libraries |
| `trapFocus?`    | `boolean`                            | `true`      | Enable focus trapping                 |
| `restoreFocus?` | `boolean`                            | `true`      | Restore focus on close                |
| `initialFocus?` | `HTMLElement \| (() => HTMLElement)` | `undefined` | Element to focus on open              |
| `finalFocus?`   | `HTMLElement \| (() => HTMLElement)` | `undefined` | Element to focus on close             |
| `ref?`          | `React.Ref<HTMLElement>`             | `undefined` | Ref forwarded to content element      |

**Events**

| Name                   | Parameters               | Description                   | When Triggered                          |
| ---------------------- | ------------------------ | ----------------------------- | --------------------------------------- |
| `onOpenAutoFocus`      | `(event: Event)`         | Before auto-focus on open     | Dialog opens                            |
| `onCloseAutoFocus`     | `(event: Event)`         | Before focus restore on close | Dialog closes                           |
| `onEscapeKeyDown`      | `(event: KeyboardEvent)` | Escape key pressed            | Escape key interaction                  |
| `onPointerDownOutside` | `(event: PointerEvent)`  | Pointer down outside content  | Outside interaction                     |
| `onInteractOutside`    | `(event: PointerEvent)`  | Interaction outside content   | Outside interaction with preventDefault |

**HTML Attributes**

| Attribute    | Value                | Purpose              |
| ------------ | -------------------- | -------------------- |
| `data-state` | `"open" \| "closed"` | Current dialog state |

### Dialog.Title

Dialog title that provides an accessible name for the dialog.

**Props**

| Name       | Type                     | Default     | Description                    |
| ---------- | ------------------------ | ----------- | ------------------------------ |
| `children` | `ReactNode`              | -           | Title content                  |
| `as?`      | `PolymorphicAs`            | `'h2'`      | Polymorphic element type       |
| `level?`   | `number`                 | `2`         | Heading level (1-6)            |
| `ref?`     | `React.Ref<HTMLElement>` | `undefined` | Ref forwarded to title element |

**Events**

| Name | Parameters | Description | When Triggered |
| ---- | ---------- | ----------- | -------------- |
| -    | -          | No events   | -              |

**HTML Attributes**

| Attribute | Value    | Purpose           |
| --------- | -------- | ----------------- |
| `id`      | `string` | Unique identifier |

### Dialog.Description

Dialog description that provides additional accessible information about the dialog.

**Props**

| Name       | Type                     | Default     | Description                          |
| ---------- | ------------------------ | ----------- | ------------------------------------ |
| `children` | `ReactNode`              | -           | Description content                  |
| `as?`      | `PolymorphicAs`            | `'p'`       | Polymorphic element type             |
| `ref?`     | `React.Ref<HTMLElement>` | `undefined` | Ref forwarded to description element |

**Events**

| Name | Parameters | Description | When Triggered |
| ---- | ---------- | ----------- | -------------- |
| -    | -          | No events   | -              |

**HTML Attributes**

| Attribute | Value    | Purpose           |
| --------- | -------- | ----------------- |
| `id`      | `string` | Unique identifier |

### Dialog.Close

Button that closes the dialog when activated.

**Props**

| Name       | Type                     | Default     | Description                           |
| ---------- | ------------------------ | ----------- | ------------------------------------- |
| `children` | `ReactNode \| ((state: DialogCloseRenderProps) => ReactNode)` | - | Close button content or render props function |
| `as?`      | `PolymorphicAs`            | `'button'`  | Polymorphic element type              |
| `ref?`     | `React.Ref<HTMLElement>` | `undefined` | Ref forwarded to close button element |

**Render Props (DialogCloseRenderProps)**

| Property | Type         | Description                                   |
| -------- | ------------ | --------------------------------------------- |
| `isOpen` | `boolean`    | Whether the dialog is currently visible       |
| `close`  | `() => void` | Function to programmatically close the dialog |

**Events**

| Name        | Parameters               | Description                 | When Triggered       |
| ----------- | ------------------------ | --------------------------- | -------------------- |
| `onClick`   | `(event: MouseEvent)`    | Close button clicked        | Mouse click          |
| `onKeyDown` | `(event: KeyboardEvent)` | Key pressed on close button | Keyboard interaction |

**HTML Attributes**

| Attribute | Value      | Purpose                                    |
| --------- | ---------- | ------------------------------------------ |
| `type`    | `"button"` | Button type for form submission prevention |

## Keyboard Interactions

Complete keyboard behavior for the entire Dialog component:

| Key           | Action            | Context               | Notes                                                  |
| ------------- | ----------------- | --------------------- | ------------------------------------------------------ |
| `Tab`         | Navigate forward  | Dialog content        | Moves focus through interactive elements within dialog |
| `Shift + Tab` | Navigate backward | Dialog content        | Moves focus backward through elements within dialog    |
| `Enter`       | Activate          | Trigger/Close buttons | Opens or closes dialog                                 |
| `Space`       | Activate          | Trigger/Close buttons | Alternative activation method for buttons              |
| `Escape`      | Close dialog      | Dialog open           | Closes dialog and returns focus to trigger             |
| `Enter`       | Activate          | Any focusable element | Triggers primary action of focused element             |
| `Space`       | Activate          | Any button element    | Triggers button action                                 |

**Focus Management:**

- When dialog opens, focus moves to the first focusable element or `initialFocus` element
- Focus is trapped within the dialog content while open
- Tab cycling is constrained to dialog content
- When dialog closes, focus returns to the trigger or `finalFocus` element
- Focus restoration can be customized or disabled via props
