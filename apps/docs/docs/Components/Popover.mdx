---
title: Popover
description: A headless popover component with intelligent positioning, focus management, and full accessibility support for creating tooltips, dropdowns, and floating panels.
---

import React from 'react';
import LiveCode from '../../src/components/LiveCode';
import AnatomyViewer from '../../src/components/AnatomyViewer/AnatomyViewer';
import { Popover } from '@turkish-technology/spar';

# Popover

A floating panel component anchored to a trigger element with smart positioning, collision detection, and comprehensive accessibility features.

## Live Demo

<div className='popover-demo-container'>
<LiveCode
  code={`<Popover.Root>
        <Popover.Trigger className='popover-trigger'>
          Open Popover
        </Popover.Trigger>
        <Popover.Content className='popover-content'>
          <Popover.Arrow className='popover-arrow' />
          <div className='popover-header'>
            <span className='popover-icon'></span>
            <span className='popover-title'>Information</span>
            <Popover.Close className='popover-close'>
              <svg xmlns="http://www.w3.org/2000/svg" width="7" height="7" viewBox="0 0 7 7" fill="none">
                <path d="M6.72003 0.157674C6.51675 -0.045608 6.18837 -0.045608 5.98509 0.157674L3.43625 2.7013L0.887404 0.152461C0.684122 -0.0508205 0.355743 -0.0508205 0.152461 0.152461C-0.0508205 0.355743 -0.0508205 0.684122 0.152461 0.887404L2.7013 3.43625L0.152461 5.98509C-0.0508205 6.18837 -0.0508205 6.51675 0.152461 6.72003C0.355743 6.92331 0.684122 6.92331 0.887404 6.72003L3.43625 4.17119L5.98509 6.72003C6.18837 6.92331 6.51675 6.92331 6.72003 6.72003C6.92331 6.51675 6.92331 6.18837 6.72003 5.98509L4.17119 3.43625L6.72003 0.887404C6.9181 0.689334 6.9181 0.355743 6.72003 0.157674Z"/>
              </svg>
            </Popover.Close>
          </div>
          <p className='popover-paragraph'>Lorem ipsum dolor sit amet consectetur. Duis cras laeet ultrices tellus. Vitae id interdum sagittis </p>
        </Popover.Content>
      </Popover.Root>`}
  cssCode={`.popover-trigger {
  border-radius: 8px;
  padding: 12px 14px;
  font-weight: 400;
  border: none;

/_ Light mode styles _/
background-color: rgba(0, 0, 0, 0.08);
color: #222530;
font-size: 13px;
}

[data-theme='dark'] .popover-trigger {
/_ Dark mode styles _/
background-color: rgba(255, 255, 255, 0.12);
color: #f2f5f8;
}

.popover-content {
width: 200px;
border-radius: 5px;
border: 0.625px solid #e1e4ea;
background-color: #ebebeb;
}

[data-theme='dark'] .popover-content {
border-color: #2b303b;
background-color: #222;
}

.popover-header {
display: flex;
align-items: center;
gap: 5px;
padding: 7.5px;
}

.popover-icon {
width: 12.5px;
height: 12.5px;
border-radius: 2.5px;
border: 0.625px solid #cacfd8;
background: rgba(0, 0, 0, 0.72);
}

[data-theme='dark'] .popover-icon {
border-color: #525866;
background-color: #c0c0c0;
}

.popover-title {
color: #222530;
font-size: 8.757px;
}

[data-theme='dark'] .popover-title {
color: #f2f5f8;
}

.popover-close {
margin-left: auto;
border: none;
cursor: pointer;
background-color: transparent;
}

.popover-close path {
fill: #222530;
}

[data-theme='dark'] path {
fill: #f2f5f8;
}

.popover-paragraph {
color: #717784;
font-size: 7.5px;
padding: 7.5px;
padding-top: 2.5px;
margin-bottom: 0px;
}

[data-theme='dark'] .popover-paragraph {
color: #99a0ae;
}

.popover-arrow {
color: #ebebeb;
}

[data-theme='dark'] .popover-arrow {
color: #222;
}`}
/>

</div>

## Features

- ✅ Smart positioning with automatic collision detection
- ✅ Controlled and uncontrolled state management
- ✅ Optional modal behavior with focus trapping
- ✅ Customizable positioning (side and alignment)
- ✅ Outside click and focus detection
- ✅ Escape key to close
- ✅ Auto-focus management on open/close
- ✅ Automatic portal rendering to document.body
- ✅ Optional decorative arrow element
- ✅ SSR-safe implementation

## Installation

```sh
pnpm install @turkish-technology/spar
```

```ts
import { Popover } from '@turkish-technology/spar';
```

---

## Anatomy

The Popover component is built as a compound component with multiple parts:

<AnatomyViewer
  parts={[
    {
      name: 'root',
      label: 'Root',
      description: 'Container managing popover state and providing context',
    },
    {
      name: 'trigger',
      label: 'Trigger',
      description: 'Interactive button that toggles popover visibility',
    },
    {
      name: 'content',
      label: 'Content',
      description: 'Floating panel that holds the popover content',
    },
    { name: 'close', label: 'Close', description: 'Optional button to close the popover' },
    { name: 'arrow', label: 'Arrow', description: 'Optional decorative arrow pointing to trigger' },
  ]}
>
  <div className='popover-demo-container'>
    <Popover.Root data-spar-part='root'>
      <Popover.Trigger data-spar-part='trigger' className='popover-trigger'>
        {'Open Popover'}
      </Popover.Trigger>
      <Popover.Content data-spar-part='content' className='popover-content'>
        <Popover.Arrow data-spar-part='arrow' />
        <div className='popover-header'>
          <span className='popover-icon'></span>
          <span className='popover-title'>{'Information'}</span>
          <Popover.Close data-spar-part='close' className='popover-close'>
            <svg
              xmlns='http://www.w3.org/2000/svg'
              width='7'
              height='7'
              viewBox='0 0 7 7'
              fill='none'
            >
              <path d='M6.72003 0.157674C6.51675 -0.045608 6.18837 -0.045608 5.98509 0.157674L3.43625 2.7013L0.887404 0.152461C0.684122 -0.0508205 0.355743 -0.0508205 0.152461 0.152461C-0.0508205 0.355743 -0.0508205 0.684122 0.152461 0.887404L2.7013 3.43625L0.152461 5.98509C-0.0508205 6.18837 -0.0508205 6.51675 0.152461 6.72003C0.355743 6.92331 0.684122 6.92331 0.887404 6.72003L3.43625 4.17119L5.98509 6.72003C6.18837 6.92331 6.51675 6.92331 6.72003 6.72003C6.92331 6.51675 6.92331 6.18837 6.72003 5.98509L4.17119 3.43625L6.72003 0.887404C6.9181 0.689334 6.9181 0.355743 6.72003 0.157674Z' />
            </svg>
          </Popover.Close>
        </div>
        <p className='popover-paragraph'>
          {
            'Lorem ipsum dolor sit amet consectetur. Duis cras laeet ultrices tellus. Vitae id interdum sagittis '
          }
        </p>
      </Popover.Content>
    </Popover.Root>
  </div>
</AnatomyViewer>

### Structure

```jsx
<Popover.Root>
  <Popover.Trigger>...</Popover.Trigger>
  <Popover.Content>
    <Popover.Close>...</Popover.Close>
  </Popover.Content>
</Popover.Root>;

{
  /* With optional parts */
}
<Popover.Root>
  <Popover.Trigger>...</Popover.Trigger>
  <Popover.Content>
    <Popover.Arrow />
    ...
  </Popover.Content>
</Popover.Root>;
```

**Data Flow:**

- `Popover.Root` manages the open/closed state and provides context to all child components
- `Popover.Trigger` toggles the popover visibility and maintains ARIA relationship with content
- `Popover.Content` positions itself intelligently relative to the trigger with collision detection
- `Popover.Close` provides a way to programmatically close the popover from within the content
- Optional `Popover.Arrow` adds a decorative visual pointer to the trigger element

- `Popover.Root`: Root context provider managing popover state
- `Popover.Trigger`: Button that toggles popover visibility
- `Popover.Content`: Floating panel with content
- `Popover.Close`: Optional button to close the popover
- `Popover.Arrow`: Optional decorative arrow element

---

## Code Examples

### Basic Popover

Simple popover with trigger and content:

```tsx
export function BasicPopover() {
  return (
    <Popover.Root>
      <Popover.Trigger>Show Info</Popover.Trigger>
      <Popover.Content>
        <p>This is popover content</p>
      </Popover.Content>
    </Popover.Root>
  );
}
```

### Controlled Popover

Popover with external state control:

```tsx
export function ControlledPopover() {
  const [open, setOpen] = React.useState(false);

  return (
    <div>
      <button onClick={() => setIsOpen(!isOpen)}>External Toggle</button>

      <Popover.Root open={open} onOpenChange={setOpen}>
        <Popover.Trigger>Open Popover</Popover.Trigger>
        <Popover.Content>
          <p>Controlled content</p>
        </Popover.Content>
      </Popover.Root>
    </div>
  );
}
```

### Modal Popover with Focus Trap

Modal popover that traps focus within the content:

```tsx
export function ModalPopover() {
  return (
    <Popover.Root modal={true}>
      <Popover.Trigger>Open Modal Popover</Popover.Trigger>
      <Popover.Content trapFocus={true}>
        <h2>Modal Popover</h2>
        <p>Focus is trapped within this popover</p>
        <button>First Focusable</button>
        <button>Second Focusable</button>
        <Popover.Close>Close</Popover.Close>
      </Popover.Content>
    </Popover.Root>
  );
}
```

### Custom Positioning

Popover with specific side and alignment preferences:

```tsx
export function PositionedPopover() {
  return (
    <Popover.Root side='top' align='start'>
      <Popover.Trigger>Top-Start Popover</Popover.Trigger>
      <Popover.Content>
        <p>Positioned at top-start with 16px offset</p>
      </Popover.Content>
    </Popover.Root>
  );
}
```

### Popover with Arrow

Popover with a decorative arrow pointing to the trigger:

```tsx
export function PopoverWithArrow() {
  return (
    <Popover.Root>
      <Popover.Trigger>Show Popover</Popover.Trigger>
      <Popover.Content>
        <Popover.Arrow />
        <p>Content with arrow</p>
      </Popover.Content>
    </Popover.Root>
  );
}
```

### With Focus Management Callbacks

Popover with custom focus management logic:

```tsx
export function PopoverWithFocusCallbacks() {
  const handleOpenAutoFocus = (event: Event) => {
    console.log('Popover opened, focus moved inside');
    // Optionally prevent default focus behavior
    // event.preventDefault();
  };

  const handleCloseAutoFocus = (event: Event) => {
    console.log('Popover closed, focus returned to trigger');
  };

  return (
    <Popover.Root>
      <Popover.Trigger>Open Popover</Popover.Trigger>
      <Popover.Content
        onOpenAutoFocus={handleOpenAutoFocus}
        onCloseAutoFocus={handleCloseAutoFocus}
      >
        <input placeholder='Auto-focused input' />
        <Popover.Close>Close</Popover.Close>
      </Popover.Content>
    </Popover.Root>
  );
}
```

### With Outside Interaction Handling

Popover with custom outside interaction logic:

```tsx
export function PopoverWithOutsideHandlers() {
  const handlePointerDownOutside = (event: React.PointerEvent) => {
    console.log('Clicked outside popover');
    // Optionally prevent default close behavior
    // event.preventDefault();
  };

  const handleEscapeKeyDown = (event: React.KeyboardEvent) => {
    console.log('Escape key pressed');
  };

  return (
    <Popover.Root>
      <Popover.Trigger>Open Popover</Popover.Trigger>
      <Popover.Content
        onPointerDownOutside={handlePointerDownOutside}
        onEscapeKeyDown={handleEscapeKeyDown}
      >
        <p>Try clicking outside or pressing Escape</p>
      </Popover.Content>
    </Popover.Root>
  );
}
```

### Custom Portal Container

Popover rendered in a specific container element:

```tsx
export function PopoverWithCustomPortal() {
  const [container, setContainer] = React.useState<HTMLElement | null>(null);

  return (
    <div>
      <div ref={setContainer} id='custom-portal' />

      <Popover.Root>
        <Popover.Trigger>Open Popover</Popover.Trigger>
        <Popover.Content container={container}>
          <p>Rendered in custom container</p>
        </Popover.Content>
      </Popover.Root>
    </div>
  );
}
```

---

## API Reference

### Popover.Root

The root container that provides context and manages state for all popover components within it.

**Props**

| Name            | Type                                     | Default     | Description                                      |
| --------------- | ---------------------------------------- | ----------- | ------------------------------------------------ |
| `open?`         | `boolean`                                | `undefined` | Controlled state for popover visibility          |
| `onOpenChange?` | `(open: boolean) => void`                | `undefined` | Callback when popover open state changes         |
| `defaultOpen?`  | `boolean`                                | `false`     | Initial open state for uncontrolled mode         |
| `modal?`        | `boolean`                                | `false`     | Whether popover should behave modally            |
| `disabled?`     | `boolean`                                | `false`     | Disables all popover triggers (prevents opening) |
| `side?`         | `"top" \| "bottom" \| "left" \| "right"` | `"bottom"`  | Preferred side for popover positioning           |
| `align?`        | `"start" \| "center" \| "end"`           | `"center"`  | Preferred alignment relative to trigger          |
| `children`      | `React.ReactNode`                        | -           | Popover.Trigger and Popover.Content components   |

**Events**

| Name           | Parameters        | Description                      | When Triggered                          |
| -------------- | ----------------- | -------------------------------- | --------------------------------------- |
| `onOpenChange` | `(open: boolean)` | State changes when opened/closed | User interaction or programmatic change |

**HTML Attributes**

| Attribute    | Value                | Purpose                 |
| ------------ | -------------------- | ----------------------- |
| `data-state` | `"open" \| "closed"` | Indicates popover state |

### Popover.Trigger

Button element that toggles the popover visibility. Extends all `Button` component props including polymorphic rendering via the `as` prop.

**Props**

| Name               | Type                                                                         | Default     | Description                                         |
| ------------------ | ---------------------------------------------------------------------------- | ----------- | --------------------------------------------------- |
| `as?`              | `React.ElementType`                                                          | `"button"`  | Element type to render as                           |
| `children`         | `React.ReactNode \| ((state: PopoverTriggerRenderProps) => React.ReactNode)` | -           | Trigger content or render props function            |
| `disabled?`        | `boolean`                                                                    | `false`     | Whether trigger is disabled                         |
| `autoFocus?`       | `boolean`                                                                    | `false`     | Whether the button should receive focus on mount    |
| `isLoading?`       | `boolean`                                                                    | `false`     | Loading state with screen reader support            |
| `isPressed?`       | `boolean`                                                                    | `undefined` | Toggle state - creates a toggle button when defined |
| `onPressedChange?` | `(pressed: boolean) => void`                                                 | `undefined` | Callback fired when toggle state changes            |
| `onClick?`         | `(event: React.MouseEvent<HTMLButtonElement>) => void`                       | `undefined` | Custom click handler                                |
| `onKeyDown?`       | `(event: React.KeyboardEvent<HTMLButtonElement>) => void`                    | `undefined` | Custom keydown handler                              |
| `ref?`             | `React.Ref<HTMLButtonElement>`                                               | `undefined` | Ref to the trigger element                          |
| `...props`         | `ButtonProps`                                                                | -           | All standard Button component attributes            |

**Render Props (PopoverTriggerRenderProps)**

| Property   | Type         | Description                                     |
| ---------- | ------------ | ----------------------------------------------- |
| `isOpen`   | `boolean`    | Whether the popover is currently visible        |
| `disabled` | `boolean`    | Whether the trigger is disabled                 |
| `open`     | `() => void` | Function to programmatically open the popover   |
| `close`    | `() => void` | Function to programmatically close the popover  |
| `toggle`   | `() => void` | Function to programmatically toggle the popover |

**Events**

| Name        | Parameters                                        | Description            | When Triggered         |
| ----------- | ------------------------------------------------- | ---------------------- | ---------------------- |
| `onClick`   | `(event: React.MouseEvent<HTMLButtonElement>)`    | Trigger clicked        | Mouse click on trigger |
| `onKeyDown` | `(event: React.KeyboardEvent<HTMLButtonElement>)` | Key pressed on trigger | Keyboard interaction   |

**HTML Attributes**

| Attribute       | Value                | Purpose                   |
| --------------- | -------------------- | ------------------------- |
| `data-state`    | `"open" \| "closed"` | Indicates popover state   |
| `data-disabled` | `string`             | Indicates disabled state  |
| `disabled`      | `boolean`            | Native disabled attribute |

### Popover.Content

Floating panel container that holds the popover content.

**Props**

| Name                    | Type                                                      | Default         | Description                                             |
| ----------------------- | --------------------------------------------------------- | --------------- | ------------------------------------------------------- |
| `side?`                 | `"top" \| "bottom" \| "left" \| "right"`                  | `"bottom"`      | Side of trigger to position against                     |
| `align?`                | `"start" \| "center" \| "end"`                            | `"center"`      | Alignment relative to trigger                           |
| `onOpenAutoFocus?`      | `(event: Event) => void`                                  | `undefined`     | Called when popover opens and focus moves inside        |
| `onCloseAutoFocus?`     | `(event: Event) => void`                                  | `undefined`     | Called when popover closes and focus returns to trigger |
| `onEscapeKeyDown?`      | `(event: React.KeyboardEvent) => void`                    | `undefined`     | Called when escape is pressed                           |
| `onPointerDownOutside?` | `(event: React.PointerEvent) => void`                     | `undefined`     | Called when pointer down occurs outside content         |
| `onFocusOutside?`       | `(event: React.FocusEvent) => void`                       | `undefined`     | Called when focus moves outside content                 |
| `onInteractOutside?`    | `(event: React.PointerEvent \| React.FocusEvent) => void` | `undefined`     | Called when interaction occurs outside content          |
| `trapFocus?`            | `boolean`                                                 | `false`         | Whether to trap focus within content                    |
| `style?`                | `React.CSSProperties`                                     | `undefined`     | Custom styles for content                               |
| `onKeyDown?`            | `(event: React.KeyboardEvent<HTMLDivElement>) => void`    | `undefined`     | Custom keydown handler                                  |
| `ref?`                  | `React.Ref<HTMLDivElement>`                               | `undefined`     | Ref to the content element                              |
| `children`              | `React.ReactNode`                                         | -               | Content to display in popover                           |
| `container?`            | `HTMLElement \| null`                                     | `document.body` | Custom portal container element                         |

**Events**

| Name                   | Parameters                                        | Description                       | When Triggered                      |
| ---------------------- | ------------------------------------------------- | --------------------------------- | ----------------------------------- |
| `onOpenAutoFocus`      | `(event: Event)`                                  | Focus moves inside on open        | Popover opens                       |
| `onCloseAutoFocus`     | `(event: Event)`                                  | Focus returns to trigger on close | Popover closes                      |
| `onEscapeKeyDown`      | `(event: React.KeyboardEvent)`                    | Escape key pressed                | User presses Escape                 |
| `onPointerDownOutside` | `(event: React.PointerEvent)`                     | Pointer down outside content      | Click/touch outside popover         |
| `onFocusOutside`       | `(event: React.FocusEvent)`                       | Focus moves outside content       | Focus leaves popover                |
| `onInteractOutside`    | `(event: React.PointerEvent \| React.FocusEvent)` | Any interaction outside content   | Click, touch, or focus outside      |
| `onKeyDown`            | `(event: React.KeyboardEvent<HTMLDivElement>)`    | Key pressed within content        | Keyboard interaction inside popover |

**HTML Attributes**

| Attribute    | Value                                    | Purpose                   |
| ------------ | ---------------------------------------- | ------------------------- |
| `id`         | `string`                                 | Unique identifier         |
| `tabIndex`   | `-1`                                     | Allows programmatic focus |
| `data-state` | `"open"`                                 | Always open when rendered |
| `data-side`  | `"top" \| "bottom" \| "left" \| "right"` | Actual positioned side    |
| `data-align` | `"start" \| "center" \| "end"`           | Actual alignment          |

### Popover.Arrow

Optional decorative arrow element that points to the trigger. Headless: renders a plain element with no visual opinions — user provides all styling.

**Props**

| Name         | Type                  | Default     | Description                        |
| ------------ | --------------------- | ----------- | ---------------------------------- |
| `as?`        | `React.ElementType`   | `'svg'`     | Polymorphic element type to render |
| `ref?`       | `React.Ref<Element>`  | `undefined` | Forward ref support                |
| `className?` | `string`              | `undefined` | CSS class names for styling        |
| `style?`     | `React.CSSProperties` | `undefined` | Custom styles for arrow            |

**Events**

| Name | Parameters | Description                | When Triggered |
| ---- | ---------- | -------------------------- | -------------- |
| None | -          | Arrow is purely decorative | -              |

**HTML Attributes**

| Attribute   | Value                                    | Purpose                   |
| ----------- | ---------------------------------------- | ------------------------- |
| `data-side` | `"top" \| "bottom" \| "left" \| "right"` | Indicates positioned side |

### Popover.Close

Close button component that automatically closes the popover when clicked. Extends all `Button` component props including polymorphic rendering via the `as` prop.

**Props**

| Name               | Type                                                                       | Default     | Description                                         |
| ------------------ | -------------------------------------------------------------------------- | ----------- | --------------------------------------------------- |
| `as?`              | `React.ElementType`                                                        | `"button"`  | Element type to render as                           |
| `children`         | `React.ReactNode \| ((state: PopoverCloseRenderProps) => React.ReactNode)` | -           | Close button content or render props                |
| `disabled?`        | `boolean`                                                                  | `false`     | Whether close button is disabled                    |
| `autoFocus?`       | `boolean`                                                                  | `false`     | Whether the button should receive focus on mount    |
| `isLoading?`       | `boolean`                                                                  | `false`     | Loading state with screen reader support            |
| `isPressed?`       | `boolean`                                                                  | `undefined` | Toggle state - creates a toggle button when defined |
| `onPressedChange?` | `(pressed: boolean) => void`                                               | `undefined` | Callback fired when toggle state changes            |
| `onClick?`         | `(event: React.MouseEvent<HTMLButtonElement>) => void`                     | `undefined` | Custom click handler                                |
| `ref?`             | `React.Ref<HTMLButtonElement>`                                             | `undefined` | Ref to the close element                            |
| `...props`         | `ButtonProps`                                                              | -           | All standard Button component attributes            |

**Render Props (PopoverCloseRenderProps)**

| Property | Type         | Description                              |
| -------- | ------------ | ---------------------------------------- |
| `isOpen` | `boolean`    | Whether the popover is currently visible |
| `close`  | `() => void` | Function to close the popover            |

**Events**

| Name      | Parameters                                     | Description          | When Triggered              |
| --------- | ---------------------------------------------- | -------------------- | --------------------------- |
| `onClick` | `(event: React.MouseEvent<HTMLButtonElement>)` | Close button clicked | Mouse click on close button |

**HTML Attributes**

| Attribute            | Value      | Purpose                    |
| -------------------- | ---------- | -------------------------- |
| `data-popover-close` | `string`   | Identifies as close button |
| `type`               | `"button"` | Native button type         |

## Keyboard Interactions

Complete keyboard behavior for the popover component:

| Key           | Action               | Context                         | Notes                                                 |
| ------------- | -------------------- | ------------------------------- | ----------------------------------------------------- |
| `Tab`         | Navigate             | Always                          | Moves focus through interactive elements in DOM order |
| `Shift + Tab` | Navigate backward    | Always                          | Moves focus backward through interactive elements     |
| `Enter`       | Toggle popover       | Focused trigger                 | Opens or closes the popover                           |
| `Space`       | Toggle popover       | Focused trigger                 | Alternative activation method for trigger             |
| `ArrowDown`   | Open popover         | Focused trigger                 | Opens popover if closed                               |
| `Escape`      | Close popover        | Popover open                    | Closes popover and returns focus to trigger           |
| `Home`        | First focusable      | Inside popover                  | Moves focus to first focusable element in popover     |
| `End`         | Last focusable       | Inside popover                  | Moves focus to last focusable element in popover      |
| `Tab`         | Cycle focus forward  | Inside popover (trapFocus=true) | Cycles through popover content, wraps at end          |
| `Shift + Tab` | Cycle focus backward | Inside popover (trapFocus=true) | Cycles through popover content, wraps at start        |
