---
title: Select
description: Fully accessible, headless select component for single-value selection with complete keyboard navigation and ARIA listbox support.
---

import React from 'react';
import LiveCode from '../../src/components/LiveCode';
import AnatomyViewer from '../../src/components/AnatomyViewer/AnatomyViewer';
import { Select } from '@turkish-technology/spar';

# Select

A headless, fully accessible dropdown component that allows users to choose a single value from a list of options with complete WAI-ARIA Listbox pattern implementation.

`Select` does not include dedicated trigger-icon or selected-indicator primitives. Add any chevron, caret, or selected marker directly inside `Select.Trigger` and `Select.Item` so the visual layer stays fully application-owned.

## Live Demo

<div className='select-demo-container'>
<LiveCode
  code={`<div className='select-demo-container'>
  <Select.Root>
    <Select.Trigger className='select-trigger'>
      <Select.Value className='select-value' placeholder='Options' />
      <span aria-hidden='true' className='select-trigger-addon'>▾</span>
    </Select.Trigger>
    <Select.Content className='select-content'>
      <Select.Item className='select-item' value='option1'>
        <Select.ItemText className='select-item-text'>Option 1</Select.ItemText>
        <span aria-hidden='true' className='select-item-check'>✓</span>
      </Select.Item>
      <Select.Item className='select-item' value='option2'>
        <Select.ItemText className='select-item-text'>Option 2</Select.ItemText>
        <span aria-hidden='true' className='select-item-check'>✓</span>
      </Select.Item>
      <Select.Item className='select-item' value='option3'>
        <Select.ItemText className='select-item-text'>Option 3</Select.ItemText>
        <span aria-hidden='true' className='select-item-check'>✓</span>
      </Select.Item>
      <Select.Arrow className='select-arrow' />
    </Select.Content>
  </Select.Root>
</div>`}
  cssCode={`.select-demo-container .select-trigger {
  min-width: 180px;
  min-height: 40px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 0 14px;
  border-radius: 10px;
  border: 1px solid #e1e4ea;
  background: rgba(0, 0, 0, 0.08);
}

[data-theme='dark'] .select-demo-container .select-trigger {
border-color: #2b303b;
background: rgba(255, 255, 255, 0.12);
}

.select-demo-container .select-value,
.select-demo-container .select-trigger-addon {
color: #222530;
font-size: 14px;
line-height: 20px;
}

[data-theme='dark'] .select-demo-container .select-value,
[data-theme='dark'] .select-demo-container .select-trigger-addon {
color: #f2f5f8;
}

.select-demo-container .select-trigger[data-state='open'] .select-trigger-addon {
transform: rotate(180deg);
}

.select-content {
--select-surface: #f7f8fa;
padding: 8px;
border-radius: 12px;
border: 1px solid #e1e4ea;
background: var(--select-surface);
box-shadow:
0 12px 32px rgba(34, 37, 48, 0.08),
0 4px 12px rgba(34, 37, 48, 0.06);
}

[data-theme='dark'] .select-content {
--select-surface: #1f232b;
border-color: #2b303b;
background: #1f232b;
}

.select-demo-container .select-item {
min-height: 36px;
padding: 0 10px;
border-radius: 8px;
cursor: pointer;
display: flex;
align-items: center;
justify-content: space-between;
gap: 12px;
color: #222530;
font-size: 14px;
line-height: 18px;
}

[data-theme='dark'] .select-demo-container .select-item {
color: #f2f5f8;
}

.select-demo-container .select-item[data-highlighted] {
background: rgba(0, 0, 0, 0.08);
outline: none;
}

[data-theme='dark'] .select-demo-container .select-item[data-highlighted] {
background: rgba(255, 255, 255, 0.12);
}

.select-demo-container .select-item[data-disabled] {
opacity: 0.5;
cursor: not-allowed;
}

.select-demo-container .select-item-check {
opacity: 0;
color: #222530;
transition: opacity 0.2s ease;
}

[data-theme='dark'] .select-demo-container .select-item-check {
color: #f2f5f8;
}

.select-demo-container .select-item[data-state='checked'] .select-item-check {
opacity: 1;
}

.select-arrow {
color: var(--select-surface);
}`}
/>

</div>

## Features

- ✅ Single-value selection (`value: string`)
- ✅ Controlled and uncontrolled usage (`value/defaultValue`, `open/defaultOpen`)
- ✅ Built-in typeahead search (default 700ms buffer)
- ✅ Positioning with `useFloating` (offset, flip, shift, hide)
- ✅ Form integration (generates a hidden input when `name` is provided)
- ✅ Portal-based content rendering (`container` or `document.body`)
- ✅ Grouping lists with groups, labels, and separators
- ✅ Disabling at the Root or Item level
- ✅ Compound structure built around Root/Trigger/Content/Item

## Installation

```tsx
import { Select } from '@turkish-technology/spar';
```

## Anatomy

`Select` is built as a compound component:

<AnatomyViewer
  parts={[
    { name: 'root', label: 'Root', description: 'Main container managing state and context' },
    { name: 'trigger', label: 'Trigger', description: 'Button that toggles dropdown visibility' },
    { name: 'value', label: 'Value', description: 'Displays selected value or placeholder' },
    {
      name: 'content',
      label: 'Content',
      description: 'Dropdown container with smart positioning',
    },
    { name: 'group', label: 'Group', description: 'Groups related items together' },
    { name: 'label', label: 'Label', description: 'Label for item groups' },
    { name: 'item', label: 'Item', description: 'Individual selectable option' },
    { name: 'itemText', label: 'ItemText', description: 'Text content of an item' },
    { name: 'separator', label: 'Separator', description: 'Visual separator between items/groups' },
    { name: 'arrow', label: 'Arrow', description: 'Optional arrow pointing to trigger' },
  ]}
>
  <div className='select-demo-container'>
    <Select.Root data-spar-part='root' defaultValue='option2'>
      <Select.Trigger data-spar-part='trigger' className='select-trigger'>
        <Select.Value data-spar-part='value' className='select-value' placeholder='Options' />
        <span aria-hidden='true' className='select-trigger-addon'>
          {'▾'}
        </span>
      </Select.Trigger>
      <Select.Content data-spar-part='content' className='select-content'>
        <Select.Item data-spar-part='item' className='select-item' value='option1'>
          <Select.ItemText data-spar-part='itemText' className='select-item-text'>
            {'Option 1'}
          </Select.ItemText>
          <span aria-hidden='true' className='select-item-check'>
            {'✓'}
          </span>
        </Select.Item>
        <Select.Item data-spar-part='item' className='select-item' value='option2'>
          <Select.ItemText data-spar-part='itemText' className='select-item-text'>
            {'Option 2'}
          </Select.ItemText>
          <span aria-hidden='true' className='select-item-check'>
            {'✓'}
          </span>
        </Select.Item>
        <Select.Item data-spar-part='item' className='select-item' value='option3'>
          <Select.ItemText data-spar-part='itemText' className='select-item-text'>
            {'Option 3'}
          </Select.ItemText>
          <span aria-hidden='true' className='select-item-check'>
            {'✓'}
          </span>
        </Select.Item>
        <Select.Arrow data-spar-part='arrow' className='select-arrow' />
      </Select.Content>
    </Select.Root>
  </div>
</AnatomyViewer>

### Structure

```tsx
<Select.Root>
  <Select.Trigger>
    <Select.Value />
  </Select.Trigger>

  <Select.Content>
    <Select.Group>
      <Select.Label />
      <Select.Item>
        <Select.ItemText />
      </Select.Item>
    </Select.Group>

    <Select.Separator />
    <Select.Arrow />
  </Select.Content>
</Select.Root>
```

**Data Flow:**

- `Select.Root` manages open/closed state and the selected value, and provides context.
- `Select.Trigger` toggles the Root state and sets the focus strategy for Content on arrow-key input.
- `Select.Value` displays the selected `Item` text (or the `placeholder`).
- `Select.Content` renders into a portal when open and manages highlight state and keyboard navigation.
- `Select.Item` updates the selection and changes the Root value through click or keyboard interaction.
- `Select.ItemText` registers the text value used for typeahead.
- `Select.Group` + `Select.Label` establish the semantic group structure.
- `Select.Separator` provides a separator role.
- `Select.Arrow` is an optional arrow positioned relative to the Content placement.

**Alternative: Tree-shakeable named imports**

```tsx
import { SelectRoot, SelectTrigger, SelectValue } from '@turkish-technology/spar';

<SelectRoot>
  <SelectTrigger>
    <SelectValue />
  </SelectTrigger>
  {/* ... */}
</SelectRoot>;
```

### Custom Visual Composition

`Select` does not include a separate icon/indicator primitive. The trigger arrow or selected marker is added as a normal React element inside the existing parts.

```tsx
<Select.Trigger className='select-trigger'>
  <Select.Value placeholder='Choose an option' />
  <span aria-hidden='true' className='select-trigger-addon'>
    ▾
  </span>
</Select.Trigger>

<Select.Item className='select-item' value='option1'>
  <Select.ItemText>Option 1</Select.ItemText>
  <span aria-hidden='true' className='select-item-check'>
    ✓
  </span>
</Select.Item>
```

## Code Examples

### Basic Controlled Select

Basic example where the open state and selected value are controlled by the parent component:

```tsx
function ControlledSelect() {
  const [selectedFruit, setSelectedFruit] = React.useState('');
  const [open, setOpen] = React.useState(false);

  return (
    <Select.Root
      value={selectedFruit}
      onValueChange={setSelectedFruit}
      open={open}
      onOpenChange={setOpen}
    >
      <Select.Trigger>
        <Select.Value placeholder='Choose a fruit' />
      </Select.Trigger>

      <Select.Content>
        <Select.Item value='apple'>
          <Select.ItemText>Apple</Select.ItemText>
        </Select.Item>
        <Select.Item value='banana'>
          <Select.ItemText>Banana</Select.ItemText>
        </Select.Item>
        <Select.Item value='orange'>
          <Select.ItemText>Orange</Select.ItemText>
        </Select.Item>
      </Select.Content>
    </Select.Root>
  );
}
```

### Grouped Items with Labels

You can section options with `Group` + `Label` + `Separator`:

```tsx
function GroupedSelect() {
  const [value, setValue] = React.useState('');

  return (
    <Select.Root value={value} onValueChange={setValue}>
      <Select.Trigger>
        <Select.Value placeholder='Select a country' />
      </Select.Trigger>

      <Select.Content>
        <Select.Group>
          <Select.Label>North America</Select.Label>
          <Select.Item value='us'>
            <Select.ItemText>United States</Select.ItemText>
          </Select.Item>
          <Select.Item value='ca'>
            <Select.ItemText>Canada</Select.ItemText>
          </Select.Item>
          <Select.Item value='mx'>
            <Select.ItemText>Mexico</Select.ItemText>
          </Select.Item>
        </Select.Group>

        <Select.Separator />

        <Select.Group>
          <Select.Label>Europe</Select.Label>
          <Select.Item value='uk'>
            <Select.ItemText>United Kingdom</Select.ItemText>
          </Select.Item>
          <Select.Item value='de'>
            <Select.ItemText>Germany</Select.ItemText>
          </Select.Item>
          <Select.Item value='fr'>
            <Select.ItemText>France</Select.ItemText>
          </Select.Item>
        </Select.Group>
      </Select.Content>
    </Select.Root>
  );
}
```

### Form Integration

When `name` is provided, Root generates a hidden input for the selected value.

```tsx
function SelectInForm() {
  const [category, setCategory] = React.useState('');

  const handleSubmit = (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const formData = new FormData(event.currentTarget);
    console.log('Category:', formData.get('category'));
  };

  return (
    <form onSubmit={handleSubmit}>
      <label htmlFor='category-select'>Product Category</label>
      <Select.Root name='category' value={category} onValueChange={setCategory} required>
        <Select.Trigger id='category-select'>
          <Select.Value placeholder='Select category' />
        </Select.Trigger>

        <Select.Content>
          <Select.Item value='electronics'>
            <Select.ItemText>Electronics</Select.ItemText>
          </Select.Item>
          <Select.Item value='clothing'>
            <Select.ItemText>Clothing</Select.ItemText>
          </Select.Item>
          <Select.Item value='books'>
            <Select.ItemText>Books</Select.ItemText>
          </Select.Item>
        </Select.Content>
      </Select.Root>
      <button type='submit'>Submit</button>
    </form>
  );
}
```

## API Reference

### Select.Root

Provides the root state and context.

**Props**

| Name             | Type                      | Default     | Description                                                       |
| ---------------- | ------------------------- | ----------- | ----------------------------------------------------------------- |
| `id?`            | `string`                  | `undefined` | Base id for ARIA relationship ids                                 |
| `value?`         | `string`                  | `undefined` | Controlled selected value                                         |
| `defaultValue?`  | `string`                  | `undefined` | Initial uncontrolled value                                        |
| `onValueChange?` | `(value: string) => void` | `undefined` | Value change callback                                             |
| `open?`          | `boolean`                 | `undefined` | Controlled open/closed state                                      |
| `defaultOpen?`   | `boolean`                 | `false`     | Initial uncontrolled open state                                   |
| `onOpenChange?`  | `(open: boolean) => void` | `undefined` | Open/closed change callback                                       |
| `disabled?`      | `boolean`                 | `false`     | Disables the entire Select                                        |
| `required?`      | `boolean`                 | `false`     | Marks the field as required for forms                             |
| `name?`          | `string`                  | `undefined` | Form field name; generates a hidden input when `value` is defined |
| `dir?`           | `'ltr' \| 'rtl'`          | `'ltr'`     | Writing direction                                                 |
| `autoFocus?`     | `boolean`                 | `false`     | Focuses the trigger on mount                                      |
| `as?`            | `React.ElementType`       | `'div'`     | Polymorphic element for Root                                      |
| `children?`      | `React.ReactNode`         | `undefined` | Root content                                                      |

**Events**

| Name            | Parameters        | Description               | When Triggered                                          |
| --------------- | ----------------- | ------------------------- | ------------------------------------------------------- |
| `onValueChange` | `(value: string)` | Selected value changes    | When an item is selected or controlled state is updated |
| `onOpenChange`  | `(open: boolean)` | Open/closed state changes | On trigger interaction or programmatic open/close calls |

**HTML Attributes**

| Attribute        | Value | Purpose                        |
| ---------------- | ----- | ------------------------------ |
| `data-disabled`  | `""`  | Added when Root is disabled    |
| `data-autofocus` | `""`  | Added when `autoFocus` is true |

### Select.Trigger

Trigger part that opens and closes the root state.

**Props**

| Name               | Type                                                                        | Default                | Description                                           |
| ------------------ | --------------------------------------------------------------------------- | ---------------------- | ----------------------------------------------------- |
| `children?`        | `React.ReactNode \| ((state: SelectTriggerRenderProps) => React.ReactNode)` | `undefined`            | Normal content or a render-props function             |
| `as?`              | `React.ElementType`                                                         | `'button'`             | Polymorphic element for Trigger                       |
| `disabled?`        | `boolean`                                                                   | Root `disabled` value  | Trigger disabled value; falls back to Root if omitted |
| `autoFocus?`       | `boolean`                                                                   | Root `autoFocus` value | Autofocus at the button level                         |
| `isLoading?`       | `boolean`                                                                   | `false`                | Button loading state (`ButtonOwnProps`)               |
| `isPressed?`       | `boolean`                                                                   | `undefined`            | Button toggle state (`ButtonOwnProps`)                |
| `onPressedChange?` | `(pressed: boolean) => void`                                                | `undefined`            | Button toggle change callback                         |
| `ref?`             | `React.Ref<HTMLElement>`                                                    | `undefined`            | Forwarded ref                                         |
| `...props`         | `ButtonProps`                                                               | -                      | Other HTML/ARIA props inherited from Button           |

**Render Props (SelectTriggerRenderProps)**

| Property   | Type                  | Description                                      |
| ---------- | --------------------- | ------------------------------------------------ |
| `isOpen`   | `boolean`             | Whether the dropdown is currently visible        |
| `value`    | `string \| undefined` | The currently selected value                     |
| `disabled` | `boolean`             | Whether the trigger is disabled                  |
| `open`     | `() => void`          | Function to programmatically open the dropdown   |
| `close`    | `() => void`          | Function to programmatically close the dropdown  |
| `toggle`   | `() => void`          | Function to programmatically toggle the dropdown |

**Events**

| Name        | Parameters                                  | Description                       | When Triggered                                   |
| ----------- | ------------------------------------------- | --------------------------------- | ------------------------------------------------ |
| `onClick`   | `(event: React.MouseEvent<HTMLElement>)`    | Trigger click/activation callback | After mouse click or Button activation           |
| `onKeyDown` | `(event: React.KeyboardEvent<HTMLElement>)` | Trigger keydown callback          | On every keydown event, before internal handling |
| `onFocus`   | `(event: React.FocusEvent<HTMLElement>)`    | Button focus callback             | When the trigger receives focus                  |
| `onBlur`    | `(event: React.FocusEvent<HTMLElement>)`    | Button blur callback              | When the trigger loses focus                     |

**HTML Attributes**

| Attribute          | Value                | Purpose                               |
| ------------------ | -------------------- | ------------------------------------- |
| `role`             | `"combobox"`         | Trigger semantic role                 |
| `aria-haspopup`    | `"listbox"`          | Type of panel that opens              |
| `aria-expanded`    | `boolean`            | Open/closed state                     |
| `aria-controls`    | `string`             | Id of the content element             |
| `aria-labelledby`  | `string`             | Reference to the value node id        |
| `aria-required`    | `true`               | Added when Root `required` is true    |
| `data-state`       | `"open" \| "closed"` | Open/closed state                     |
| `data-required`    | `""`                 | Added when Root `required` is true    |
| `data-placeholder` | `""`                 | Added when there is no selected value |
| `data-disabled`    | `""`                 | Button disabled state                 |
| `data-loading`     | `""`                 | Button loading state                  |
| `data-pressed`     | `""`                 | Button toggle pressed state           |
| `data-autofocus`   | `""`                 | Button autofocus state                |

### Select.Value

Displays the selected Item text or the placeholder.

**Props**

| Name           | Type                | Default     | Description                                               |
| -------------- | ------------------- | ----------- | --------------------------------------------------------- |
| `placeholder?` | `React.ReactNode`   | `undefined` | Content shown when there is no selection                  |
| `as?`          | `React.ElementType` | `'span'`    | Polymorphic element for Value                             |
| `children?`    | `React.ReactNode`   | `undefined` | If provided, renders instead of placeholder/selected text |

**Events**

| Name | Parameters | Description                                    | When Triggered |
| ---- | ---------- | ---------------------------------------------- | -------------- |
| -    | -          | No component-specific events on the Value part | -              |

**HTML Attributes**

| Attribute | Value    | Purpose                                                               |
| --------- | -------- | --------------------------------------------------------------------- |
| `id`      | `string` | Assigned automatically for the Trigger `aria-labelledby` relationship |

### Select.Content

Popup content layer. It does not render when closed.

**Props**

| Name                    | Type                                     | Default         | Description                                                              |
| ----------------------- | ---------------------------------------- | --------------- | ------------------------------------------------------------------------ |
| `side?`                 | `'top' \| 'right' \| 'bottom' \| 'left'` | `'bottom'`      | Preferred placement side                                                 |
| `align?`                | `'start' \| 'center' \| 'end'`           | `'center'`      | Alignment relative to the Trigger                                        |
| `container?`            | `HTMLElement \| null`                    | `document.body` | Portal target                                                            |
| `onEscapeKeyDown?`      | `(event: KeyboardEvent) => void`         | `undefined`     | Escape key callback (`preventDefault` can stop closing)                  |
| `onPointerDownOutside?` | `(event: PointerEvent) => void`          | `undefined`     | Outside click callback (`preventDefault` can stop closing)               |
| `onCloseAutoFocus?`     | `(event: FocusEvent) => void`            | `undefined`     | Prop type exists; the current implementation does not call this callback |
| `as?`                   | `React.ElementType`                      | `'div'`         | Polymorphic element for Content                                          |
| `ref?`                  | `React.Ref<HTMLElement>`                 | `undefined`     | Forwarded ref                                                            |
| `children?`             | `React.ReactNode`                        | `undefined`     | Content children                                                         |

**Events**

| Name                   | Parameters                                     | Description              | When Triggered                                    |
| ---------------------- | ---------------------------------------------- | ------------------------ | ------------------------------------------------- |
| `onKeyDown`            | `(event: React.KeyboardEvent<HTMLDivElement>)` | Content keydown callback | When keydown occurs while Content is focused      |
| `onEscapeKeyDown`      | `(event: KeyboardEvent)`                       | Escape callback          | When `Escape` is pressed while Content is focused |
| `onPointerDownOutside` | `(event: PointerEvent)`                        | Outside click callback   | When pointer down happens outside Trigger/Content |

**HTML Attributes**

| Attribute         | Value                                    | Purpose                                |
| ----------------- | ---------------------------------------- | -------------------------------------- |
| `id`              | `string`                                 | ARIA connection to the Trigger         |
| `role`            | `"listbox"`                              | Listbox semantics                      |
| `aria-labelledby` | `string`                                 | Reference to the Trigger id            |
| `tabIndex`        | `-1`                                     | Programmatic focus for the popup panel |
| `data-state`      | `"open"`                                 | Content renders only while open        |
| `data-side`       | `"top" \| "right" \| "bottom" \| "left"` | Computed floating side                 |
| `data-align`      | `"start" \| "center" \| "end"`           | Computed floating alignment            |

**Positioning Notes**

- `useFloating` middleware: `offset(8)`, `flip()`, `shift({ padding: 8 })`, `hide()`
- `minWidth` is matched to the trigger width
- `Select.Arrow` is positioned automatically through context when rendered inside Content

### Select.Item

Selectable option part.

**Props**

| Name         | Type                                                                     | Default     | Description                                                        |
| ------------ | ------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------ |
| `value`      | `string`                                                                 | -           | Item value (required)                                              |
| `disabled?`  | `boolean`                                                                | `false`     | Disables the item                                                  |
| `textValue?` | `string`                                                                 | `undefined` | Typeahead text; falls back to text registered by `Select.ItemText` |
| `children?`  | `React.ReactNode \| ((state: SelectItemRenderProps) => React.ReactNode)` | `undefined` | Normal content or render-props                                     |
| `as?`        | `React.ElementType`                                                      | `'div'`     | Polymorphic element for Item                                       |
| `ref?`       | `React.Ref<HTMLElement>`                                                 | `undefined` | Forwarded ref                                                      |

**Render Props (SelectItemRenderProps)**

| Property        | Type         | Description                                   |
| --------------- | ------------ | --------------------------------------------- |
| `isSelected`    | `boolean`    | Whether this item is currently selected       |
| `isHighlighted` | `boolean`    | Whether this item is currently highlighted    |
| `disabled`      | `boolean`    | Whether this item is disabled                 |
| `select`        | `() => void` | Function to programmatically select this item |

**Events**

| Name            | Parameters                                    | Description           | When Triggered                                                              |
| --------------- | --------------------------------------------- | --------------------- | --------------------------------------------------------------------------- |
| `onClick`       | `(event: React.MouseEvent<HTMLDivElement>)`   | Item click callback   | When the item is clicked (selection occurs unless `preventDefault` is used) |
| `onPointerMove` | `(event: React.PointerEvent<HTMLDivElement>)` | Pointer move callback | When the pointer moves over the item (updates highlight state)              |

**HTML Attributes**

| Attribute          | Value                      | Purpose                            |
| ------------------ | -------------------------- | ---------------------------------- |
| `role`             | `"option"`                 | Option semantics                   |
| `aria-selected`    | `boolean`                  | Selected item state                |
| `aria-disabled`    | `true`                     | Added when the item is disabled    |
| `data-state`       | `"checked" \| "unchecked"` | Selected/unselected state          |
| `data-disabled`    | `""`                       | Added when the item is disabled    |
| `data-highlighted` | `""`                       | Added when the item is highlighted |

### Select.ItemText

Provides the item text content.

**Props**

| Name        | Type                | Default     | Description                      |
| ----------- | ------------------- | ----------- | -------------------------------- |
| `as?`       | `React.ElementType` | `'span'`    | Polymorphic element for ItemText |
| `children?` | `React.ReactNode`   | `undefined` | Item text content                |

**Events**

| Name | Parameters | Description                                       | When Triggered |
| ---- | ---------- | ------------------------------------------------- | -------------- |
| -    | -          | No component-specific events on the ItemText part | -              |

**HTML Attributes**

| Attribute | Value | Purpose                                                               |
| --------- | ----- | --------------------------------------------------------------------- |
| -         | -     | No fixed HTML/data attributes are added by the component for ItemText |

### Select.Group

Item grouping part.

**Props**

| Name        | Type                | Default     | Description                   |
| ----------- | ------------------- | ----------- | ----------------------------- |
| `as?`       | `React.ElementType` | `'div'`     | Polymorphic element for Group |
| `children?` | `React.ReactNode`   | `undefined` | Group content                 |

**Events**

| Name | Parameters | Description                                    | When Triggered |
| ---- | ---------- | ---------------------------------------------- | -------------- |
| -    | -          | No component-specific events on the Group part | -              |

**HTML Attributes**

| Attribute         | Value     | Purpose                         |
| ----------------- | --------- | ------------------------------- |
| `role`            | `"group"` | Group semantics                 |
| `aria-labelledby` | `string`  | Reference to the group label id |

### Select.Label

Group label part.

**Props**

| Name        | Type                | Default     | Description                   |
| ----------- | ------------------- | ----------- | ----------------------------- |
| `as?`       | `React.ElementType` | `'label'`   | Polymorphic element for Label |
| `children?` | `React.ReactNode`   | `undefined` | Label content                 |

**Events**

| Name | Parameters | Description                                    | When Triggered |
| ---- | ---------- | ---------------------------------------------- | -------------- |
| -    | -          | No component-specific events on the Label part | -              |

**HTML Attributes**

| Attribute | Value    | Purpose                                   |
| --------- | -------- | ----------------------------------------- |
| `id`      | `string` | Receives the `labelId` generated by Group |

### Select.Separator

Separator part between groups or items.

**Props**

| Name        | Type                | Default     | Description                       |
| ----------- | ------------------- | ----------- | --------------------------------- |
| `as?`       | `React.ElementType` | `'div'`     | Polymorphic element for Separator |
| `children?` | `React.ReactNode`   | `undefined` | Optional content                  |

**Events**

| Name | Parameters | Description                                        | When Triggered |
| ---- | ---------- | -------------------------------------------------- | -------------- |
| -    | -          | No component-specific events on the Separator part | -              |

**HTML Attributes**

| Attribute          | Value          | Purpose               |
| ------------------ | -------------- | --------------------- |
| `role`             | `"separator"`  | Separator semantics   |
| `aria-orientation` | `"horizontal"` | Indicates orientation |

### Select.Arrow

Optional arrow part positioned according to the Content/Trigger placement.

**Props**

| Name         | Type                  | Default     | Description                                                    |
| ------------ | --------------------- | ----------- | -------------------------------------------------------------- |
| `as?`        | `React.ElementType`   | `'svg'`     | Polymorphic element for Arrow                                  |
| `ref?`       | `React.Ref<Element>`  | `undefined` | Forwarded ref                                                  |
| `children?`  | `React.ReactNode`     | `undefined` | Custom content instead of the default polygon when using `svg` |
| `className?` | `string`              | `undefined` | Class name                                                     |
| `style?`     | `React.CSSProperties` | `undefined` | Style override                                                 |

**Events**

| Name | Parameters | Description                                    | When Triggered |
| ---- | ---------- | ---------------------------------------------- | -------------- |
| -    | -          | No component-specific events on the Arrow part | -              |

**HTML Attributes**

| Attribute        | Value                                       | Purpose                                 |
| ---------------- | ------------------------------------------- | --------------------------------------- |
| `aria-hidden`    | `"true"`                                    | Hides it from assistive technologies    |
| `data-placement` | `"top" \| "top-start" \| ... \| "left-end"` | Provides computed placement information |
| `width`          | `10`                                        | Only for the default `svg`              |
| `height`         | `5`                                         | Only for the default `svg`              |
| `viewBox`        | `"0 0 10 5"`                                | Only for the default `svg`              |

**Note:** For arrow positioning, `Select.Arrow` should be used inside `Select.Content`.

### SelectTriggerRenderProps

| Name       | Type                  | Description             |
| ---------- | --------------------- | ----------------------- |
| `isOpen`   | `boolean`             | Root open/closed state  |
| `value`    | `string \| undefined` | Selected value          |
| `disabled` | `boolean`             | Trigger disabled state  |
| `open`     | `() => void`          | Open programmatically   |
| `close`    | `() => void`          | Close programmatically  |
| `toggle`   | `() => void`          | Toggle programmatically |

### SelectItemRenderProps

| Name            | Type         | Description                         |
| --------------- | ------------ | ----------------------------------- |
| `isSelected`    | `boolean`    | Whether the item is selected        |
| `isHighlighted` | `boolean`    | Whether the item is highlighted     |
| `disabled`      | `boolean`    | Whether the item is disabled        |
| `select`        | `() => void` | Function to select programmatically |

## Keyboard Interactions

Keyboard behavior in the current implementation:

| Key                | Action                                                 | Context         | Notes                                                |
| ------------------ | ------------------------------------------------------ | --------------- | ---------------------------------------------------- |
| `Enter` / `Space`  | Toggle open/close                                      | Trigger focused | Works through the `Button` activation flow           |
| `ArrowDown`        | Open and set focus strategy to the selected/first item | Trigger focused | Only opens when currently closed                     |
| `ArrowUp`          | Open and set focus strategy to the selected/first item | Trigger focused | Only opens when currently closed                     |
| `Escape`           | Close and return focus to Trigger                      | Content focused | Unless `preventDefault` is used in `onEscapeKeyDown` |
| `Enter` / `Space`  | Select the highlighted item, close, and focus Trigger  | Content focused | No selection happens if nothing is highlighted       |
| `ArrowDown`        | Highlight the next enabled item                        | Content focused | Wraps from the end of the list to the start          |
| `ArrowUp`          | Highlight the previous enabled item                    | Content focused | Wraps from the start of the list to the end          |
| `Home` / `PageUp`  | Highlight the first enabled item                       | Content focused | -                                                    |
| `End` / `PageDown` | Highlight the last enabled item                        | Content focused | -                                                    |
| `Tab`              | Close the Content                                      | Content focused | Calls `event.preventDefault()`                       |
| `A-Z` / `a-z`      | Highlight the matching enabled item via typeahead      | Content focused | Default timeout is `700ms`                           |
