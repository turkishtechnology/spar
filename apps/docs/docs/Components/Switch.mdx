---
title: Switch
description: Headless switch primitives for on/off settings with accessible ARIA semantics, form integration, and controlled or uncontrolled state.
---

import React from 'react';
import LiveCode from '../../src/components/LiveCode';
import AnatomyViewer from '../../src/components/AnatomyViewer/AnatomyViewer';
import { Switch } from '@turkish-technology/spar';

# Switch

Headless switch primitives for creating accessible on/off settings with custom track, thumb, label, and hint parts.

## Live Demo

<div className='switch-demo-container'>
<LiveCode
  code={`function SwitchDemo() {
  const [checked, setChecked] = React.useState(true);

return (

<div className='switch-demo-container'>
<Switch.Root className='switch-root' checked={checked} onChange={setChecked}>
<Switch.Control className='switch-control'>
<Switch.Track className='switch-track'>
<Switch.Thumb className='switch-thumb' />
</Switch.Track>
</Switch.Control>
<Switch.Label className='switch-label'>Notifications</Switch.Label>
</Switch.Root>
</div>
);
}`}
  cssCode={`.switch-demo-container .switch-root {
display: inline-flex;
align-items: center;
gap: 12px;
}

.switch-demo-container .switch-control {
width: 56px;
height: 32px;
padding: 0;
border: 0;
background: transparent;
display: inline-flex;
align-items: center;
cursor: pointer;
}

.switch-demo-container .switch-control:focus-visible {
outline: 2px solid #d43347;
outline-offset: 3px;
border-radius: 999px;
}

.switch-demo-container .switch-track {
box-sizing: border-box;
width: 56px;
height: 32px;
padding: 4px;
border: 1px solid #e1e4ea;
border-radius: 999px;
background-color: rgba(0, 0, 0, 0.08);
display: flex;
align-items: center;
transition: background-color 0.2s ease;
}

.switch-demo-container .switch-track[data-state='checked'] {
background-color: #d43347;
border-color: #d43347;
}

.switch-demo-container .switch-thumb {
width: 22px;
height: 22px;
border-radius: 50%;
background: rgba(0, 0, 0, 0.72);
transition: transform 0.2s ease, background-color 0.2s ease;
}

.switch-demo-container .switch-track[data-state='checked'] .switch-thumb {
transform: translateX(24px);
background: #fff;
}

.switch-demo-container .switch-label {
font-weight: 500;
}

[data-theme='dark'] .switch-demo-container .switch-track {
border-color: #2b303b;
background-color: rgba(255, 255, 255, 0.12);
}

[data-theme='dark'] .switch-demo-container .switch-thumb {
background: rgba(255, 255, 255, 0.72);
}

[data-theme='dark'] .switch-demo-container .switch-track[data-state='checked'] {
background-color: #d43347;
border-color: #d43347;
}`}/>

</div>

The live demo uses the compound API so the switch behavior, visual track, thumb, and label can be styled independently.

## Features

- ✅ Compound parts for root, control, track, thumb, label, and hint
- ✅ Supports both controlled (`checked`) and uncontrolled (`defaultChecked`) state
- ✅ Integrates with native forms through a hidden checkbox input (`name`, `value`, `form`, `required`)
- ✅ Automatically wires `Switch.Label` and `Switch.Hint` to the control
- ✅ Supports render-prop children for programmatic state updates
- ✅ Supports disabled and read-only interaction modes
- ✅ Supports polymorphic rendering via the `as` prop

## Import

```tsx
import { Switch } from '@turkish-technology/spar';
```

```tsx
import {
  Switch,
  SwitchRoot,
  SwitchControl,
  SwitchTrack,
  SwitchThumb,
  SwitchLabel,
  SwitchHint,
} from '@turkish-technology/spar/switch';
```

## Anatomy

The Switch component is built as a compound component. `Switch.Root` owns state and context, while `Switch.Control` carries the interactive switch semantics.

<AnatomyViewer
  parts={[
    {
      name: 'root',
      label: 'Root',
      description: 'State and context container',
    },
    {
      name: 'control',
      label: 'Control',
      description: 'Interactive element with role="switch"',
    },
    {
      name: 'track',
      label: 'Track',
      description: 'Visual switch track',
    },
    {
      name: 'thumb',
      label: 'Thumb',
      description: 'Visual movable thumb',
    },
    {
      name: 'label',
      label: 'Label',
      description: 'Visible label registered with the control',
    },
    {
      name: 'hint',
      label: 'Hint',
      description: 'Description registered with the control',
    },
  ]}
>
  <div className='switch-demo-container'>
    <Switch.Root data-spar-part='root' className='switch-root' defaultChecked>
      <Switch.Control data-spar-part='control' className='switch-control'>
        <Switch.Track data-spar-part='track' className='switch-track'>
          <Switch.Thumb data-spar-part='thumb' className='switch-thumb' />
        </Switch.Track>
      </Switch.Control>
      <div>
        <Switch.Label data-spar-part='label' className='switch-label'>
          {'Flight alerts'}
        </Switch.Label>
        <Switch.Hint data-spar-part='hint' className='switch-hint'>
          {'Only operational alerts are sent.'}
        </Switch.Hint>
      </div>
    </Switch.Root>
  </div>
</AnatomyViewer>

### Structure

```tsx
<Switch.Root>
  <Switch.Control>
    <Switch.Track>
      <Switch.Thumb />
    </Switch.Track>
  </Switch.Control>
  <Switch.Label />
  <Switch.Hint />
</Switch.Root>
```

**Data Flow:**

- `Switch.Root` manages controlled or uncontrolled checked state and provides context
- `Switch.Control` receives switch ARIA attributes and toggles state on click, `Space`, and `Enter`
- `Switch.Label` registers its id as the control label and toggles the switch when clicked
- `Switch.Hint` registers its id as the control description
- When `name` is provided on `Switch.Root`, `Switch.Control` renders a synchronized hidden checkbox input

## Code Examples

### Basic Usage

Use the compound parts when you want a custom visual switch with an accessible label.

```tsx
function BasicSwitch() {
  return (
    <Switch.Root defaultChecked>
      <Switch.Control>
        <Switch.Track>
          <Switch.Thumb />
        </Switch.Track>
      </Switch.Control>
      <Switch.Label>Enable notifications</Switch.Label>
    </Switch.Root>
  );
}
```

### Controlled Switch

Control the checked value from parent state when the setting needs to stay in sync with other UI.

```tsx {2,5-6}
function ControlledSwitch() {
  const [enabled, setEnabled] = React.useState(false);

  return (
    <Switch.Root checked={enabled} onChange={setEnabled}>
      <Switch.Control>
        <Switch.Track>
          <Switch.Thumb />
        </Switch.Track>
      </Switch.Control>
      <Switch.Label>Dark mode</Switch.Label>
      <Switch.Hint>{enabled ? 'Theme is dark' : 'Theme is light'}</Switch.Hint>
    </Switch.Root>
  );
}
```

### Uncontrolled Switch

Use `defaultChecked` for simple cases where the switch can manage its own state internally.

```tsx {3}
function UncontrolledSwitch() {
  return (
    <Switch.Root defaultChecked={false} onChange={(checked) => console.log('Changed to:', checked)}>
      <Switch.Control>
        <Switch.Track>
          <Switch.Thumb />
        </Switch.Track>
      </Switch.Control>
      <Switch.Label>Auto-managed switch</Switch.Label>
    </Switch.Root>
  );
}
```

### Form Integration

Provide `name` and optional `value` on `Switch.Root` when the switch should submit as part of a native form.

```tsx {2-3,7-10,13,22}
function FormExample() {
  const [notifications, setNotifications] = React.useState(false);
  const [darkMode, setDarkMode] = React.useState(true);

  return (
    <form
      onSubmit={(event) => {
        event.preventDefault();
        const formData = new FormData(event.currentTarget);
        console.log(formData.get('notifications'), formData.get('darkMode'));
      }}
    >
      <Switch.Root name='notifications' checked={notifications} onChange={setNotifications}>
        <Switch.Control>
          <Switch.Track>
            <Switch.Thumb />
          </Switch.Track>
        </Switch.Control>
        <Switch.Label>Notifications</Switch.Label>
      </Switch.Root>

      <Switch.Root name='darkMode' checked={darkMode} onChange={setDarkMode} value='enabled'>
        <Switch.Control>
          <Switch.Track>
            <Switch.Thumb />
          </Switch.Track>
        </Switch.Control>
        <Switch.Label>Dark mode</Switch.Label>
      </Switch.Root>

      <button type='submit'>Save</button>
    </form>
  );
}
```

### Accessibility with Labels and Hints

Use `Switch.Label` and `Switch.Hint` when the control should expose visible text as its accessible name and description.

```tsx {6-15}
function AccessibleSwitch() {
  const [isOn, setIsOn] = React.useState(false);

  return (
    <Switch.Root checked={isOn} onChange={setIsOn} required>
      <Switch.Control>
        <Switch.Track>
          <Switch.Thumb />
        </Switch.Track>
      </Switch.Control>
      <Switch.Label>Wi-Fi connection</Switch.Label>
      <Switch.Hint>{isOn ? 'Connected to Wi-Fi' : 'Disconnected from Wi-Fi'}</Switch.Hint>
    </Switch.Root>
  );
}
```

### Disabled and Read-only States

Use `disabled` to block interaction entirely, or `readOnly` when users may inspect the value but should not change it.

```tsx {2,13,24}
function StateVariations() {
  const [enabled, setEnabled] = React.useState(true);

  return (
    <div>
      <Switch.Root checked={enabled} onChange={setEnabled}>
        <Switch.Control>
          <Switch.Track>
            <Switch.Thumb />
          </Switch.Track>
        </Switch.Control>
        <Switch.Label>Normal switch</Switch.Label>
      </Switch.Root>

      <Switch.Root defaultChecked disabled>
        <Switch.Control>
          <Switch.Track>
            <Switch.Thumb />
          </Switch.Track>
        </Switch.Control>
        <Switch.Label>Disabled on</Switch.Label>
      </Switch.Root>

      <Switch.Root defaultChecked readOnly>
        <Switch.Control>
          <Switch.Track>
            <Switch.Thumb />
          </Switch.Track>
        </Switch.Control>
        <Switch.Label>Read-only on</Switch.Label>
      </Switch.Root>
    </div>
  );
}
```

### Custom Element Rendering

The `as` prop lets you choose custom elements while preserving the same state and accessibility model.

```tsx {2,5}
function CustomElement() {
  return (
    <Switch.Root as='section' defaultChecked>
      <Switch.Control as='div'>
        <Switch.Track>
          <Switch.Thumb />
        </Switch.Track>
      </Switch.Control>
      <Switch.Label>Custom element switch</Switch.Label>
    </Switch.Root>
  );
}
```

### Standalone Shortcut

The standalone `<Switch>` component remains available for simple single-element usage. Use the compound parts when you need separate track, thumb, label, or hint elements.

```tsx
function StandaloneSwitch() {
  const [enabled, setEnabled] = React.useState(false);

  return (
    <Switch checked={enabled} onChange={setEnabled}>
      {enabled ? 'On' : 'Off'}
    </Switch>
  );
}
```

## API Reference

### Switch.Root

State provider and layout wrapper for compound switch parts.

| Name              | Type                                                                 | Default     | Description                                         |
| ----------------- | -------------------------------------------------------------------- | ----------- | --------------------------------------------------- |
| `as?`             | `React.ElementType`                                                  | `"div"`     | Element or component to render as                   |
| `checked?`        | `boolean`                                                            | `undefined` | Controlled checked state                            |
| `defaultChecked?` | `boolean`                                                            | `false`     | Initial checked state in uncontrolled mode          |
| `onChange?`       | `(checked: boolean) => void`                                         | `undefined` | Called when checked state changes                   |
| `disabled?`       | `boolean`                                                            | `false`     | Prevents interaction                                |
| `name?`           | `string`                                                             | `undefined` | Hidden input name for form submission               |
| `value?`          | `string`                                                             | `"on"`      | Hidden input value when checked                     |
| `form?`           | `string`                                                             | `undefined` | Form id used by hidden input                        |
| `required?`       | `boolean`                                                            | `false`     | Marks hidden input and switch semantics as required |
| `readOnly?`       | `boolean`                                                            | `false`     | Prevents state changes while keeping focus behavior |
| `id?`             | `string`                                                             | generated   | Used to derive the default control id               |
| `children?`       | `React.ReactNode \| ((state: SwitchRenderProps) => React.ReactNode)` | `undefined` | Static content or render-prop function              |
| `...props`        | `ComponentPropsWithRef<"div">`                                       | `-`         | Other native props for the polymorphic root element |

### Switch.Control

Interactive element that receives `role="switch"` and toggles the root checked state.

| Name                | Type                              | Default     | Description                                    |
| ------------------- | --------------------------------- | ----------- | ---------------------------------------------- |
| `as?`               | `React.ElementType`               | `"button"`  | Element or component to render as              |
| `id?`               | `string`                          | generated   | Overrides the generated control id             |
| `type?`             | `button` attributes               | `"button"`  | Native button type when rendered as a button   |
| `aria-label?`       | `string`                          | `undefined` | Accessible name override                       |
| `aria-labelledby?`  | `string`                          | label id    | Overrides the registered `Switch.Label` id     |
| `aria-describedby?` | `string`                          | hint id     | Merged with the registered `Switch.Hint` id    |
| `children?`         | `React.ReactNode`                 | `undefined` | Visual switch content, usually track and thumb |
| `...props`          | `ComponentPropsWithRef<"button">` | `-`         | Other native props for the polymorphic control |

When `Switch.Root` has a `name`, `Switch.Control` also renders a hidden synchronized `input[type="checkbox"]` for native form submission.

### Switch.Track

Visual track part. It is marked `aria-hidden="true"` and mirrors root state with data attributes.

| Name        | Type                            | Default     | Description                              |
| ----------- | ------------------------------- | ----------- | ---------------------------------------- |
| `as?`       | `React.ElementType`             | `"span"`    | Element or component to render as        |
| `children?` | `React.ReactNode`               | `undefined` | Usually contains `Switch.Thumb`          |
| `...props`  | `ComponentPropsWithRef<"span">` | `-`         | Other native props for the track element |

### Switch.Thumb

Visual thumb part. It is marked `aria-hidden="true"` and mirrors root state with data attributes.

| Name        | Type                            | Default     | Description                              |
| ----------- | ------------------------------- | ----------- | ---------------------------------------- |
| `as?`       | `React.ElementType`             | `"span"`    | Element or component to render as        |
| `children?` | `React.ReactNode`               | `undefined` | Optional visual content                  |
| `...props`  | `ComponentPropsWithRef<"span">` | `-`         | Other native props for the thumb element |

### Switch.Label

Visible label part. It registers itself as the control label and toggles the switch when clicked unless the root is disabled or read-only.

| Name        | Type                            | Default     | Description                              |
| ----------- | ------------------------------- | ----------- | ---------------------------------------- |
| `as?`       | `React.ElementType`             | `"span"`    | Element or component to render as        |
| `id?`       | `string`                        | generated   | Overrides the generated label id         |
| `children?` | `React.ReactNode`               | `undefined` | Visible label content                    |
| `...props`  | `ComponentPropsWithRef<"span">` | `-`         | Other native props for the label element |

### Switch.Hint

Description part. It registers itself as the control description.

| Name        | Type                            | Default     | Description                             |
| ----------- | ------------------------------- | ----------- | --------------------------------------- |
| `as?`       | `React.ElementType`             | `"span"`    | Element or component to render as       |
| `id?`       | `string`                        | generated   | Overrides the generated hint id         |
| `children?` | `React.ReactNode`               | `undefined` | Visible description content             |
| `...props`  | `ComponentPropsWithRef<"span">` | `-`         | Other native props for the hint element |

### Standalone Switch

The standalone `Switch` export is a single interactive element for compatibility and compact use cases. It supports the same state, form, render-prop, disabled, read-only, and polymorphic props as the previous single-part API.

**Render Props (SwitchRenderProps)**

| Property     | Type                         | Description                                        |
| ------------ | ---------------------------- | -------------------------------------------------- |
| `checked`    | `boolean`                    | Whether the switch is currently checked            |
| `setChecked` | `(checked: boolean) => void` | Function to programmatically set the checked state |
| `disabled`   | `boolean`                    | Whether the switch is disabled                     |
| `readOnly`   | `boolean`                    | Whether the switch is read-only                    |
| `isFocused`  | `boolean`                    | Whether the switch currently has focus             |
| `isHovered`  | `boolean`                    | Whether the switch is being hovered                |
| `isPressed`  | `boolean`                    | Whether the switch is being pressed                |

## HTML Attributes

| Part              | Attribute          | Value                      | Purpose                                      |
| ----------------- | ------------------ | -------------------------- | -------------------------------------------- |
| `Switch.Root`     | `data-switch-root` | `""`                       | Root marker attribute                        |
| `Switch.Root`     | `data-state`       | `"checked" \| "unchecked"` | Checked state token                          |
| `Switch.Control`  | `role`             | `"switch"`                 | Applies switch semantics                     |
| `Switch.Control`  | `aria-checked`     | `boolean`                  | Reflects checked state                       |
| `Switch.Control`  | `aria-labelledby`  | `string`                   | Registered label id or explicit override     |
| `Switch.Control`  | `aria-describedby` | `string`                   | Registered hint id plus explicit description |
| `Switch.Control`  | `aria-readonly`    | `true`                     | Present when `readOnly` is `true`            |
| `Switch.Control`  | `aria-required`    | `true`                     | Present when `required` is `true`            |
| `Switch.Control`  | `data-switch`      | `""`                       | Control marker attribute                     |
| `Switch.Control`  | `data-state`       | `"checked" \| "unchecked"` | Checked state token                          |
| `Switch.Track`    | `aria-hidden`      | `"true"`                   | Keeps decorative track out of the AT tree    |
| `Switch.Thumb`    | `aria-hidden`      | `"true"`                   | Keeps decorative thumb out of the AT tree    |
| `Track` / `Thumb` | `data-state`       | `"checked" \| "unchecked"` | Mirrors checked state for styling            |
| `Label` / `Hint`  | `data-disabled`    | `""`                       | Present when disabled                        |
| `Label` / `Hint`  | `data-readonly`    | `""`                       | Present when read-only                       |
| `Label` / `Hint`  | `data-required`    | `""`                       | Present when required                        |

Stateful parts also expose `data-checked`, `data-disabled`, `data-readonly`, `data-required`, `data-focus`, `data-hover`, and `data-active` when those states apply.

## Hidden Input

`Switch.Control` renders a hidden input only when `name` is provided on `Switch.Root`.

| Attribute     | Value        | Purpose                                  |
| ------------- | ------------ | ---------------------------------------- |
| `type`        | `"checkbox"` | Enables native form submission semantics |
| `name`        | `string`     | Form field name                          |
| `value`       | `string`     | Submitted value when checked             |
| `form`        | `string`     | Associates input to external form id     |
| `required`    | `boolean`    | Participates in native form validation   |
| `checked`     | `boolean`    | Mirrors switch checked state             |
| `disabled`    | `boolean`    | Mirrors disabled state                   |
| `tabIndex`    | `-1`         | Keeps hidden input out of tab sequence   |
| `aria-hidden` | `true`       | Hides input from assistive tech tree     |

## Keyboard Interactions

| Key           | Action                | Context          | Notes                                                   |
| ------------- | --------------------- | ---------------- | ------------------------------------------------------- |
| `Tab`         | Moves focus to switch | Focus management | Available when switch is not disabled                   |
| `Shift + Tab` | Moves focus backward  | Focus management | Standard reverse tab navigation                         |
| `Space`       | Toggles checked state | Switch focused   | No state change when `disabled` or `readOnly` is `true` |
| `Enter`       | Toggles checked state | Switch focused   | No state change when `disabled` or `readOnly` is `true` |
